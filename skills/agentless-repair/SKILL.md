---
name: agentless-repair
description: 两阶段 Bug 修复流水线 — 先精确定位（关键词检索 + LLM 重排到行级），再并行生成多候选 patch 用测试筛选最优，而非单链自我修复迭代。当存在明确复现路径的 Bug、单链自我修复陷入循环、或需要高置信度修复时激活。比 Reflexion 迭代更可靠：等价算力下多候选过滤的修复成功率显著高于单链迭代。
tags:
  [
    bug-fix,
    localization,
    patch-candidates,
    agentless,
    systematic-repair,
    regression-oracle,
    production
  ]
---

# Agentless Repair — 两阶段 Bug 修复流水线

> 理论来源：Agentless（Zhang et al., arXiv:2407.01489），在 SWE-bench Lite 上以 27.33% 解决率超越所有 Agent-based 方法（对比 SWE-agent 的 12.5%）。核心洞察：**简单的并行采样 + 测试过滤，往往优于复杂的单链 Agent 迭代。**

## 激活摘要

**何时激活**：

- 存在明确的 Bug 复现步骤或失败测试
- 单链自我修复连续 2+ 轮无进展（`feedback-loop` 的退化信号）
- 需要高置信度修复（生产 hotfix、关键路径 bug）
- 错误消息 / 堆栈跟踪明确可定位

**与相邻 skill 的分工**：

| 场景                     | 使用哪个                             |
| ------------------------ | ------------------------------------ |
| 调试根因未明             | `systematic-debugging`（先定位根因） |
| 根因已明，需可靠修复     | **`agentless-repair`（本 skill）**   |
| 无 UI 系统的业务场景验证 | `feedback-loop`（驱动器闭环）        |

**检查清单**：

- [ ] 有可复现的失败路径（失败测试或复现步骤）
- [ ] 已完成阶段一：定位到可疑文件和行号范围
- [ ] 已生成 ≥ 2 个差异化 patch 候选
- [ ] 用测试/复现步骤筛选出最优候选
- [ ] 最优候选通过回归验证（不引入新失败）

**硬约束**：

- 禁止在"只修改了措辞"的循环里重复尝试；每个候选必须有实质不同的修复策略
- 测试筛选必须实际运行，不能靠主观判断选候选
- 最终选定候选必须通过回归检查（现有测试全绿）

---

## 阶段一：精确定位

### 1.1 多信号检索

同时使用多个信号定位可疑文件（按可信度排序）：

```bash
# 信号 1：错误消息关键词
grep -rn "错误关键词" src/ --include="*.py" | head -20

# 信号 2：失败测试的导入链
grep -rn "被测函数名" src/ --include="*.py"

# 信号 3：最近修改的相关文件
git log --oneline --since="7 days ago" -- src/ | head -10
git diff HEAD~5 --name-only | grep -E "相关模块"
```

### 1.2 LLM 行级精确定位

把检索到的候选文件喂给 LLM，要求输出精确的可疑行号范围：

```
定位提示模板：
  错误信息: [完整堆栈跟踪]
  失败测试: [测试代码]
  候选文件: [文件内容，带行号]

  任务：找出最可能导致上述错误的代码行范围（3-10 行），
  并说明推断理由。输出格式：
  文件: <path>
  行范围: <start>-<end>
  推断理由: <1-2句>
```

### 1.3 定位验证

在继续之前确认定位可信度：

- 定位到的代码与错误堆栈逻辑一致 ✓
- 可疑行在最近的 git 变更中出现过（高可信） ✓
- 单元测试覆盖了该行（可通过覆盖率报告确认） ✓

---

## 阶段二：并行多候选修复

### 2.1 生成差异化候选

针对定位到的代码区域，生成 **2-3 个策略实质不同** 的修复方向：

| 候选 | 策略方向                                 | 适用场景         |
| ---- | ---------------------------------------- | ---------------- |
| P1   | **防御式修复**：在调用处加 null/边界检查 | 输入未被正确验证 |
| P2   | **根因修复**：修改产生错误数据的上游逻辑 | 数据生产端有问题 |
| P3   | **协议修复**：修改调用方对函数的使用方式 | 接口契约不匹配   |

**候选生成提示模板**：

```
定位区域代码：[代码 + 行号]
错误现象：[描述]
已知约束：[不能修改的接口/API]

为每个候选提供：
1. 修复的代码 diff
2. 修复策略（1句话说明思路差异）
3. 该策略可能失效的场景（诚实评估）
```

### 2.2 测试筛选

对每个候选运行测试，记录结果：

```bash
# 对每个候选依次：
git stash              # 保存当前状态
# 应用候选 patch
npm test -- --testPathPattern="相关测试" 2>&1 | tail -5
# 记录：PASS/FAIL 数量
git stash pop
```

**候选对比表**（每次填写）：

| 候选 | 原失败测试 | 新引入失败 | 净改善 | 策略   |
| ---- | ---------- | ---------- | ------ | ------ |
| P1   | 通过       | 0          | +1     | 防御式 |
| P2   | 失败       | —          | 0      | 根因   |
| P3   | 通过       | 1          | 0      | 协议   |

选净改善最高且不引入新失败的候选。

### 2.3 回归验证（Regression Oracle）

选定候选后执行三层回归检查：

```
Layer 1 — 变更影响域（静态）
  → grep 找出被修改函数的所有调用点
  → 确认调用点的行为预期未被破坏

Layer 2 — 差分验证（动态）
  → 对非修复路径运行 before/after 对比
  → 任何非预期输出变化都需要解释

Layer 3 — 全量回归
  → npm test / pytest / mvn test（全量，不只是相关测试）
  → 0 新增失败才可提交
```

---

## 阶段三：防退化提交

```bash
# 提交前最终检查
git diff --stat          # 确认修改范围符合预期
npm test                 # 全量绿灯
git add -p               # 精确暂存，不带入无关变更
git commit -m "fix(<scope>): <描述>

修复策略: <P1/P2/P3 + 一句话>
根因: <一句话>
验证: 全量测试通过，无新增失败"
```

---

## 与 auto-cli 集成

| 注入时机        | 说明                                                                 |
| --------------- | -------------------------------------------------------------------- |
| PHASE 2 PLAN    | bug 修复策略下，quest-designer 优先使用两阶段流水线而非单链修复      |
| PHASE 3 EXECUTE | 每个 patch 候选作为独立 Quest 并行执行                               |
| PHASE 4 VERIFY  | `test` gate 必须包含三层回归验证；`self-verification` 核查无新增失败 |
| PHASE 6 LEARN   | 成功的定位策略和候选对比结果沉淀到 patterns.md                       |

## 参考 Skills

- **systematic-debugging** — 根因未明时先调用（agentless-repair 在根因定位后接手）
- **feedback-loop** — 无 UI 系统用 CLI 驱动器执行候选验证
- **spec-driven** — bug 修复前先定义验收契约，确保修复方向正确
