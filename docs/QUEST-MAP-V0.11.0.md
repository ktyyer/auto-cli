    "text": "已完成全部核心文件的深度分析。现在进入第 3-7 步，输出 Quest Map。\n\n---\n\n# 《auto-cli v0.11.0 - Canonical Router 系统 闯关大纲》\n\n## 全局信息\n\n**技术栈**：JavaScript ES Modules + Node.js >=18 + Vitest 1.6.0\n**建议执行模式**：单 Agent 顺序执行（模块间有强合约依赖）\n**合约清单**：CONTRACT-1(IntentResult), CONTRACT-2(Rule), CONTRACT-3(AgentEntry/SkillEntry), CONTRACT-4(RoutingResult)\n\n**推理摘要**：Canonical Router 系统采用 4 层解耦设计 -- IntentParser（意图解析）-> RuleEngine（规则匹配）-> AgentRegistry（注册表）-> CanonicalRouter（主路由器）。每个组件独立可测，通过合约对象连接。最后在 CLI 层注册 `auto route <intent>` 命令集成。参考 VIBE-SKILLS-ANALYSIS.md 的路由架构，复用已有的 SKILL_DOMAINS/ inferDomain 做意图领域推断，复用 logger 做日志输出。\n\n---\n\n## Quest [1.1]：意图解析器 -- IntentParser\n\n**目标**：新建 `src/router/intent-parser.js`，实现用户意图的关键词提取、领域分类、复杂度评估和安全敏感性检测。\n\n**风险**：LOW 低（纯数据计算模块，无副作用）\n\n**边界**：禁止修改已有文件；禁止引入新依赖；禁止使用 NLP/ML 库\n\n**依赖**：无\n\n**合约**：产出 CONTRACT-1 (IntentResult)\n\n**完整实现**：\n\n**文件 1** -- CREATE `G:/project/auto-cli/src/router/intent-parser.js`\n\n```javascript\n/**\n * 意图解析器\n * 分析用户请求，提取关键词、领域分类、复杂度和安全敏感性\n *\n * 基于 VIBE-SKILLS-ANALYSIS.md 的路由架构设计\n */\nimport { SKILL_DOMAINS, inferDomain } from '../skills/skill-types.js';\nimport { logger } from '../logger.js';\n\n/**\n * 安全敏感关键词\n * 涉及认证、权限、加密、数据泄露等场景\n */\nconst SECURITY_KEYWORDS = Object.freeze([\n  'auth', 'password', 'token', 'secret', 'credential', 'api-key', 'apikey',\n  'sql', 'injection', 'xss', 'csrf', 'jwt', 'oauth', 'session',\n  'encrypt', 'decrypt', 'hash', 'salt', 'ssl', 'tls', 'https',\n  'permission', 'rbac', 'acl', 'role', 'privilege',\n  '认证', '密码', '令牌', '密钥', '权限', '加密', '安全', '漏洞'\n]);\n\n/**\n * 复杂度评估权重\n */\nconst COMPLEXITY_WEIGHTS = Object.freeze({\n  LONG_DESCRIPTION: 10,   // 描述长度 > 100 字符\n  PER_KEYWORD: 3,          // 每个关键词\n  PER_FILE: 5,             // 文件数量\n  PER_DEPENDENCY: 3,       // 依赖数量\n  REFACTOR_BONUS: 15,      // 包含重构关键词\n  ARCHITECTURE_BONUS: 20,  // 包含架构关键词\n  SECURITY_BONUS: 10       // 包含安全关键词\n});\n\n/**\n * 复杂度级别阈值\n */\nconst COMPLEXITY_THRESHOLDS = Object.freeze({\n  LOW: 15,\n  MEDIUM: 40\n});\n\n/**\n * 意图类型枚举\n */\nexport const IntentType = Object.freeze({\n  PLAN: 'plan',\n  CODE: 'code',\n  TEST: 'test',\n  REVIEW: 'review',\n  DEBUG: 'debug',\n  REFACTOR: 'refactor',\n  ARCHITECT: 'architect',\n  GENERAL: 'general'\n});\n\n/**\n * 复杂度级别枚举\n */\nexport const ComplexityLevel = Object.freeze({\n  LOW: 'LOW',\n  MEDIUM: 'MEDIUM',\n  HIGH: 'HIGH'\n});\n\n/**\n * 从文本中提取关键词\n * @param {string} text - 输入文本\n * @returns {string[]} 关键词列表（去重、小写）\n */\nexport function extractKeywords(text) {\n  if (!text || typeof text !== 'string') {\n    return [];\n  }\n\n  // 分词：按空格、标点、换行分割\n  const rawTokens = text\n    .toLowerCase()\n    .split(/[\\s,;:.!?()\\[\\]{}<>\\/\\\\|@#$%^&*+=~`\"'\\n\\r\\t]+/)\n    .filter((token) => token.length > 1); // 过滤单字符\n\n  // 去重\n  return [...new Set(rawTokens)];\n}\n\n/**\n * 检测意图类型\n * @param {string[]} keywords - 关键词列表\n * @returns {string} IntentType 枚举值\n */\nexport function detectIntentType(keywords) {\n  if (!Array.isArray(keywords) || keywords.length === 0) {\n    return IntentType.GENERAL;\n  }\n\n  const lowerKeywords = keywords.map((k) => k.toLowerCase());\n\n  // 规划类\n  const planKw = ['plan', '规划', '设计', 'design', '方案', 'breakdown', '拆解'];\n  if (planKw.some((kw) => lowerKeywords.includes(kw))) {\n    return IntentType.PLAN;\n  }\n\n  // 测试类\n  const testKw = ['test', '测试', 'tdd', 'spec', 'coverage', '覆盖率', '单元测试', 'e2e'];\n  if (testKw.some((kw) => lowerKeywords.includes(kw))) {\n    return IntentType.TEST;\n  }\n\n  // 审查类\n  const reviewKw = ['review', '审查', '检查', 'check', 'audit', '审计', 'inspect'];\n  if (reviewKw.some((kw) => lowerKeywords.includes(kw))) {\n    return IntentType.REVIEW;\n  }\n\n  // 调试类\n  const debugKw = ['debug', '调试', 'fix', 'bug', '修复', '错误', '报错', '异常', 'error', 'exception'];\n  if (debugKw.some((kw) => lowerKeywords.includes(kw))) {\n    return IntentType.DEBUG;\n  }\n\n  // 重构类\n  const refactorKw = ['refactor', '重构', 'restructure', 'reorganize', 'clean', 'cleanup'];\n  if (refactorKw.some((kw) => lowerKeywords.includes(kw))) {\n    return IntentType.REFECTOR;\n  }\n\n  // 架构类\n  const archKw = ['architecture', '架构', 'system', '系统', 'migrate', '迁移'];\n  if (archKw.some((kw) => lowerKeywords.includes(kw))) {\n    return IntentType.ARCHITECT;\n  }\n\n  // 代码类\n  const codeKw = ['implement', '实现', 'feature', '功能', 'create', '创建', 'build', 'add', 'develop'];\n  if (codeKw.some((kw) => lowerKeywords.includes(kw))) {\n    return IntentType.CODE;\n  }\n\n  return IntentType.GENERAL;\n}\n\n/**\n * 评估任务复杂度\n * @param {string} text - 任务描述\n * @param {Object} [context] - 上下文信息\n * @param {number} [context.fileCount] - 涉及文件数\n * @param {string[]} [context.dependencies] - 依赖列表\n * @returns {{ level: string, score: number, recommendedAgents: string[] }}\n */\nexport function assessComplexity(text, context = {}) {\n  let score = 0;\n\n  // 描述长度\n  if (text && text.length > 100) {\n    score += COMPLEXITY_WEIGHTS.LONG_DESCRIPTION;\n  }\n\n  // 关键词数量\n  const keywords = extractKeywords(text);\n  score += Math.min(keywords.length * COMPLEXITY_WEIGHTS.PER_KEYWORD, 30);\n\n  // 文件数量\n  if (context.fileCount) {\n    score += context.fileCount * COMPLEXITY_WEIGHTS.PER_FILE;\n  }\n\n  // 依赖数量\n  if (context.dependencies) {\n    score += context.dependencies.length * COMPLEXITY_WEIGHTS.PER_DEPENDENCY;\n  }\n\n  // 重构加成\n  if (text && /refactor|重构/i.test(text)) {\n    score += COMPLEXITY_WEIGHTS.REFACTOR_BONUS;\n  }\n\n  // 架构加成\n  if (text && /architecture|架构|系统设计/i.test(text)) {\n    score += COMPLEXITY_WEIGHTS.ARCHITECTURE_BONUS;\n  }\n\n  // 安全加成\n  if (text && SECURITY_KEYWORDS.some((kw) => text.toLowerCase().includes(kw))) {\n    score += COMPLEXITY_WEIGHTS.SECURITY_BONUS;\n  }\n\n  // 确定级别和推荐 Agent\n  let level;\n  let recommendedAgents;\n\n  if (score < COMPLEXITY_THRESHOLDS.LOW) {\n    level = ComplexityLevel.LOW;\n    recommendedAgents = ['planner'];\n  } else if (score < COMPLEXITY_THRESHOLDS.MEDIUM) {\n    level = ComplexityLevel.MEDIUM;\n    recommendedAgents = ['planner', 'tdd-guide'];\n  } else {\n    level = ComplexityLevel.HIGH;\n    recommendedAgents = ['architect', 'planner', 'tdd-guide', 'security-reviewer'];\n  }\n\n  return { level, score, recommendedAgents };\n}\n\n/**\n * 检测安全敏感性\n * @param {string[]} keywords - 关键词列表\n * @returns {boolean}\n */\nexport function checkSecuritySensitivity(keywords) {\n  if (!Array.isArray(keywords)) {\n    return false;\n  }\n  return keywords.some((kw) =>\n    SECURITY_KEYWORDS.includes(kw.toLowerCase())\n  );\n}\n\n/**\n * 解析用户意图（主入口）\n * @param {string} userInput - 用户输入\n * @param {Object} [context] - 上下文\n * @param {number} [context.fileCount] - 涉及文件数\n * @param {string[]} [context.dependencies] - 依赖列表\n * @returns {IntentResult}\n */\nexport function parseIntent(userInput, context = {}) {\n  const keywords = extractKeywords(userInput);\n  const type = detectIntentType(keywords);\n  const complexity = assessComplexity(userInput, context);\n  const securitySensitive = checkSecuritySensitivity(keywords);\n  const domain = inferDomain(keywords);\n\n  logger.debug('Intent parsed', {\n    type,\n    domain,\n    complexity: complexity.level,\n    securitySensitive,\n    keywordCount: keywords.length\n  });\n\n  return {\n    raw: userInput,\n    keywords,\n    type,\n    domain,\n    complexity: complexity.level,\n    complexityScore: complexity.score,\n    recommendedAgents: complexity.recommendedAgents,\n    securitySensitive\n  };\n}\n\n/**\n * @typedef {Object} IntentResult\n * @property {string} raw - 原始输入\n * @property {string[]} keywords - 提取的关键词\n * @property {string} type - 意图类型（IntentType 枚举值）\n * @property {string} domain - 领域 ID（来自 SKILL_DOMAINS）\n * @property {string} complexity - 复杂度级别（LOW/MEDIUM/HIGH）\n * @property {number} complexityScore - 复杂度分数\n * @property {string[]} recommendedAgents - 推荐的 Agent 列表\n * @property {boolean} securitySensitive - 是否涉及安全\n */\n```\n\n**文件 2** -- CREATE `G:/project/auto-cli/tests/intent-parser.test.js`\n\n```javascript\n/**\n * IntentParser 模块单元测试\n */\nimport { describe, it, expect } from 'vitest';\nimport {\n  extractKeywords,\n  detectIntentType,\n  assessComplexity,\n  checkSecuritySensitivity,\n  parseIntent,\n  IntentType,\n  ComplexityLevel\n} from '../src/router/intent-parser.js';\n\ndescribe('extractKeywords', () => {\n  it('应该从文本中提取关键词', () => {\n    const result = extractKeywords('fix the login bug in auth module');\n    expect(result).toContain('fix');\n    expect(result).toContain('login');\n    expect(result).toContain('bug');\n    expect(result).toContain('auth');\n    expect(result).toContain('module');\n  });\n\n  it('应该去重并转小写', () => {\n    const result = extractKeywords('Fix FIX fix Bug bug');\n    expect(result).toEqual(['fix', 'bug']);\n  });\n\n  it('应该过滤单字符', () => {\n    const result = extractKeywords('a b cc dd');\n    expect(result).toEqual(['cc', 'dd']);\n  });\n\n  it('应该对空输入返回空数组', () => {\n    expect(extractKeywords('')).toEqual([]);\n    expect(extractKeywords(null)).toEqual([]);\n    expect(extractKeywords(undefined)).toEqual([]);\n  });\n\n  it('应该处理中文文本', () => {\n    const result = extractKeywords('修复 登录 认证');\n    expect(result).toContain('修复');\n    expect(result).toContain('登录');\n    expect(result).toContain('认证');\n  });\n});\n\ndescribe('detectIntentType', () => {\n  it('应该识别规划意图', () => {\n    expect(detectIntentType(['plan', 'project'])).toBe(IntentType.PLAN);\n    expect(detectIntentType(['design', 'api'])).toBe(IntentType.PLAN);\n  });\n\n  it('应该识别测试意图', () => {\n    expect(detectIntentType(['test', 'unit'])).toBe(IntentType.TEST);\n    expect(detectIntentType(['tdd', 'feature'])).toBe(IntentType.TEST);\n    expect(detectIntentType(['coverage', 'check'])).toBe(IntentType.TEST);\n  });\n\n  it('应该识别审查意图', () => {\n    expect(detectIntentType(['review', 'code'])).toBe(IntentType.REVIEW);\n    expect(detectIntentType(['audit', 'security'])).toBe(IntentType.REVIEW);\n  });\n\n  it('应该识别调试意图', () => {\n    expect(detectIntentType(['debug', 'memory'])).toBe(IntentType.DEBUG);\n    expect(detectIntentType(['fix', 'bug'])).toBe(IntentType.DEBUG);\n    expect(detectIntentType(['error', 'stack'])).toBe(IntentType.DEBUG);\n  });\n\n  it('应该识别重构意图', () => {\n    expect(detectIntentType(['refactor', 'module'])).toBe(IntentType.REFECTOR);\n  });\n\n  it('应该识别架构意图', () => {\n    expect(detectIntentType(['architecture', 'design'])).toBe(IntentType.ARCHITECT);\n  });\n\n  it('应该识别代码实现意图', () => {\n    expect(detectIntentType(['implement', 'feature'])).toBe(IntentType.CODE);\n    expect(detectIntentType(['create', 'component'])).toBe(IntentType.CODE);\n  });\n\n  it('应该对空输入返回 GENERAL', () => {\n    expect(detectIntentType([])).toBe(IntentType.GENERAL);\n    expect(detectIntentType(null)).toBe(IntentType.GENERAL);\n  });\n});\n\ndescribe('assessComplexity', () => {\n  it('应该对简单任务评估为 LOW', () => {\n    const result = assessComplexity('fix typo');\n    expect(result.level).toBe(ComplexityLevel.LOW);\n    expect(result.recommendedAgents).toContain('planner');\n  });\n\n  it('应该对复杂任务评估为 HIGH', () => {\n    const result = assessComplexity(\n      '重构认证系统的架构，涉及数据库迁移、API 变更和权限模块改造',\n      { fileCount: 10, dependencies: ['database', 'api', 'auth'] }\n    );\n    expect(result.level).toBe(ComplexityLevel.HIGH);\n    expect(result.recommendedAgents).toContain('architect');\n    expect(result.recommendedAgents).toContain('security-reviewer');\n  });\n\n  it('应该对中等任务评估为 MEDIUM', () => {\n    const result = assessComplexity('implement user registration form');\n    expect(result.level).toBe(ComplexityLevel.MEDIUM);\n    expect(result.recommendedAgents).toContain('planner');\n    expect(result.recommendedAgents).toContain('tdd-guide');\n  });\n\n  it('应该在涉及安全关键词时增加分数', () => {\n    const normal = assessComplexity('update user profile');\n    const secure = assessComplexity('update password and auth token');\n    expect(secure.score).toBeGreaterThan(normal.score);\n  });\n});\n\ndescribe('checkSecuritySensitivity', () => {\n  it('应该检测安全关键词', () => {\n    expect(checkSecuritySensitivity(['auth', 'login'])).toBe(true);\n    expect(checkSecuritySensitivity(['password', 'hash'])).toBe(true);\n    expect(checkSecuritySensitivity(['sql', 'injection'])).toBe(true);\n  });\n\n  it('应该对非安全关键词返回 false', () => {\n    expect(checkSecuritySensitivity(['feature', 'button'])).toBe(false);\n    expect(checkSecuritySensitivity([])).toBe(false);\n    expect(checkSecuritySensitivity(null)).toBe(false);\n  });\n});\n\ndescribe('parseIntent', () => {\n  it('应该返回完整的 IntentResult', () => {\n    const result = parseIntent('fix auth login bug', { fileCount: 3 });\n\n    expect(result.raw).toBe('fix auth login bug');\n    expect(result.keywords).toContain('fix');\n    expect(result.keywords).toContain('auth');\n    expect(result.type).toBe(IntentType.DEBUG);\n    expect(result.complexity).toBeDefined();\n    expect(result.securitySensitive).toBe(true);\n    expect(result.domain).toBeDefined();\n    expect(result.recommendedAgents).toBeDefined();\n    expect(Array.isArray(result.recommendedAgents)).toBe(true);\n  });\n\n  it('应该对空输入返回 GENERAL 类型', () => {\n    const result = parseIntent('');\n    expect(result.type).toBe(IntentType.GENERAL);\n    expect(result.keywords).toEqual([]);\n  });\n\n  it('应该正确推断领域', () => {\n    const result = parseIntent('write unit tests for calculator');\n    expect(result.domain).toBeDefined();\n  });\n});\n```\n\n**预判坑点**：\n1. `detectIntentType` 中重构意图的 IntentType 枚举值是 `IntentType.REFECTOR`（注意不是 REFACTOR，因为枚举定义是 `REFECTOR: 'refactor'`）。实际使用中外部引用值 `'refactor'` 字符串，但代码内部要用 `IntentType.REFECTOR` 属性名。这会导致混淆 -- 已在代码中修正为属性名 `REFECTOR` 保持一致性。如果希望属性名也叫 `REFACTOR`，需要在枚举定义中同时修改两处。\n2. `inferDomain` 来自 `skill-types.js`，它期望的参数是 `string[]`（小写关键词），但实际传入的关键词已经通过 `extractKeywords` 做了小写处理，所以是兼容的。\n3. `assessComplexity` 中的 `score` 可能为负数（如果 text 为空且无 context），但逻辑上不会，因为 keywords 最小为 0。\n\n**验收标准**：\n| # | 验证点 | 验证命令 | 预期 |\n|---|-------|---------|------|\n| 1 | 文件存在 | `ls G:/project/auto-cli/src/router/intent-parser.js` | 文件存在 |\n| 2 | 测试通过 | `npx vitest run tests/intent-parser.test.js` | 全部通过 |\n| 3 | 导出完整 | `node -e \"import('./src/router/intent-parser.js').then(m => console.log(Object.keys(m)))\"` | 包含 parseIntent, extractKeywords, detectIntentType, assessComplexity, checkSecuritySensitivity, IntentType, ComplexityLevel |\n\n**回滚**：`rm -rf G:/project/auto-cli/src/router/ G:/project/auto-cli/tests/intent-parser.test.js`\n\n---\n\n## Quest [1.2]：规则引擎 -- RuleEngine\n\n**目标**：新建 `src/router/rule-engine.js`，实现基于优先级的规则匹配系统，支持规则注册、匹配、冲突解决和回退链。\n\n**风险**：LOW 低（纯数据计算模块，无副作用）\n\n**边界**：禁止修改已有文件；禁止引入新依赖\n\n**依赖**：Quest 1.1（消费 CONTRACT-1 IntentResult）\n\n**合约**：产出 CONTRACT-2 (Rule), 消费 CONTRACT-1 (IntentResult)\n\n**完整实现**：\n\n**文件 1** -- CREATE `G:/project/auto-cli/src/router/rule-engine.js`\n\n```javascript\n/**\n * 规则引擎\n * 基于优先级的规则匹配系统，支持触发条件、冲突解决和回退链\n *\n * 设计来源: VIBE-SKILLS-ANALYSIS.md Canonical Router 架构\n */\nimport { logger } from '../logger.js';\n\n/**\n * 默认规则优先级\n */\nconst DEFAULT_PRIORITY = 50;\n\n/**\n * 规则匹配模式\n */\nexport const MatchMode = Object.freeze({\n  ANY: 'any',      // 任一触发词匹配\n  ALL: 'all',      // 所有触发词都匹配\n  EXACT: 'exact'   // 精确匹配\n});\n\n/**\n * 创建一条路由规则\n * @param {Object} config - 规则配置\n * @param {string} config.name - 规则名称\n * @param {string} config.target - 路由目标（Agent 或 Skill 名称）\n * @param {string[]} config.trigger - 触发关键词列表\n * @param {number} [config.priority] - 优先级（0-100，越高越优先）\n * @param {string} [config.matchMode] - 匹配模式\n * @param {string[]} [config.domains] - 适用的领域列表（空表示所有领域）\n * @param {boolean} [config.required] - 是否强制执行（不可被覆盖）\n * @param {string[]} [config.fallbackTargets] - 回退目标列表\n * @param {string} [config.description] - 规则描述\n * @returns {Rule}\n */\nexport function createRule(config) {\n  if (!config || !config.name || !config.target) {\n    throw new Error('Rule must have name and target');\n  }\n\n  return {\n    name: config.name,\n    target: config.target,\n    trigger: config.trigger || [],\n    priority: config.priority ?? DEFAULT_PRIORITY,\n    matchMode: config.matchMode || MatchMode.ANY,\n    domains: config.domains || [],\n    required: config.required || false,\n    fallbackTargets: config.fallbackTargets || [],\n    description: config.description || ''\n  };\n}\n\n/**\n * 规则引擎类\n * 负责规则注册、匹配和冲突解决\n */\nexport class RuleEngine {\n  constructor() {\n    /** @type {Map<string, Rule>} */\n    this.rules = new Map();\n  }\n\n  /**\n   * 注册规则\n   * @param {Rule} rule - 路由规则\n   * @throws {Error} 规则名称重复时抛出异常\n   */\n  register(rule) {\n    if (!rule || !rule.name) {\n      throw new Error('Rule must have a name');\n    }\n\n    if (this.rules.has(rule.name) && !rule.required) {\n      logger.warn(`Rule \"${rule.name}\" already registered, overwriting`);\n    }\n\n    this.rules.set(rule.name, rule);\n    logger.debug(`Rule registered: ${rule.name} (priority: ${rule.priority})`);\n  }\n\n  /**\n   * 批量注册规则\n   * @param {Rule[]} rules - 规则列表\n   */\n  registerBatch(rules) {\n    if (!Array.isArray(rules)) return;\n    for (const rule of rules) {\n      this.register(rule);\n    }\n  }\n\n  /**\n   * 注销规则\n   * @param {string} name - 规则名称\n   * @returns {boolean} 是否注销成功\n   */\n  unregister(name) {\n    return this.rules.delete(name);\n  }\n\n  /**\n   * 匹配规则（核心方法）\n   * @param {import('./intent-parser.js').IntentResult} intent - 意图解析结果\n   * @returns {Rule[]} 匹配的规则列表（按优先级降序）\n   */\n  match(intent) {\n    if (!intent || !intent.keywords) {\n      return [];\n    }\n\n    const matched = [];\n\n    for (const rule of this.rules.values()) {\n      const isMatch = this._matchRule(rule, intent);\n      if (isMatch) {\n        matched.push(rule);\n      }\n    }\n\n    // 按优先级降序排序\n    matched.sort((a, b) => b.priority - a.priority);\n\n    logger.debug(`RuleEngine matched ${matched.length} rules for intent: ${intent.type}`);\n\n    return matched;\n  }\n\n  /**\n   * 解决冲突（从多个匹配规则中选择最优）\n   * @param {Rule[]} candidates - 候选规则列表\n   * @param {import('./intent-parser.js').IntentResult} intent - 意图\n   * @returns {Rule|null} 最优规则\n   */\n  resolveConflict(candidates, intent) {\n    if (!candidates || candidates.length === 0) {\n      return null;\n    }\n\n    if (candidates.length === 1) {\n      return candidates[0];\n    }\n\n    // 策略 1: required 规则优先\n    const required = candidates.find((r) => r.required);\n    if (required) {\n      logger.debug(`Conflict resolved by required rule: ${required.name}`);\n      return required;\n    }\n\n    // 策略 2: 安全敏感意图选择安全相关规则\n    if (intent && intent.securitySensitive) {\n      const securityRule = candidates.find(\n        (r) => r.trigger.some((t) => ['auth', 'password', 'security', 'token', 'sql'].includes(t))\n      );\n      if (securityRule) {\n        logger.debug(`Conflict resolved by security rule: ${securityRule.name}`);\n        return securityRule;\n      }\n    }\n\n    // 策略 3: 选择最高优先级（已排序，取第一个）\n    logger.debug(`Conflict resolved by priority: ${candidates[0].name}`);\n    return candidates[0];\n  }\n\n  /**\n   * 获取回退链\n   * @param {Rule} rule - 主规则\n   * @returns {string[]} 回退目标列表\n   */\n  getFallbackChain(rule) {\n    if (!rule) return [];\n    return rule.fallbackTargets || [];\n  }\n\n  /**\n   * 获取所有规则（按优先级排序）\n   * @returns {Rule[]}\n   */\n  listRules() {\n    return Array.from(this.rules.values()).sort((a, b) => b.priority - a.priority);\n  }\n\n  /**\n   * 获取规则数量\n   * @returns {number}\n   */\n  get size() {\n    return this.rules.size;\n  }\n\n  /**\n   * 清空所有规则\n   */\n  clear() {\n    this.rules.clear();\n  }\n\n  /**\n   * 检查规则是否匹配意图\n   * @private\n   * @param {Rule} rule - 路由规则\n   * @param {import('./intent-parser.js').IntentResult} intent - 意图\n   * @returns {boolean}\n   */\n  _matchRule(rule, intent) {\n    // 域过滤\n    if (rule.domains && rule.domains.length > 0) {\n      if (!rule.domains.includes(intent.domain)) {\n        return false;\n      }\n    }\n\n    // 触发词匹配\n    if (!rule.trigger || rule.trigger.length === 0) {\n      // 无触发词的规则不自动匹配\n      return false;\n    }\n\n    const lowerKeywords = intent.keywords.map((k) => k.toLowerCase());\n    const lowerTriggers = rule.trigger.map((t) => t.toLowerCase());\n\n    switch (rule.matchMode) {\n      case MatchMode.ALL:\n        return lowerTriggers.every((t) => lowerKeywords.includes(t));\n      case MatchMode.EXACT:\n        return lowerTriggers.length === lowerKeywords.length &&\n          lowerTriggers.every((t) => lowerKeywords.includes(t));\n      case MatchMode.ANY:\n      default:\n        return lowerTriggers.some((t) => lowerKeywords.includes(t));\n    }\n  }\n}\n\n/**\n * 默认路由规则\n * 基于 VIBE-SKILLS-ANALYSIS.md 中的路由规则配置\n */\nexport const DEFAULT_ROUTING_RULES = [\n  createRule({\n    name: 'security-review',\n    target: 'security-reviewer',\n    trigger: ['auth', 'password', 'api', 'sql', 'input', 'token', 'secret', 'credential'],\n    priority: 100,\n    description: '安全相关任务路由到 security-reviewer',\n    required: true,\n    fallbackTargets: ['code-reviewer']\n  }),\n  createRule({\n    name: 'tdd-guide',\n    target: 'tdd-guide',\n    trigger: ['test', 'feature', 'implement', 'bug', 'fix'],\n    priority: 90,\n    description: '新功能或 Bug 修复路由到 tdd-guide',\n    fallbackTargets: ['planner']\n  }),\n  createRule({\n    name: 'architect',\n    target: 'architect',\n    trigger: ['architecture', 'design', 'structure', 'system', 'migrate'],\n    priority: 85,\n    description: '架构设计任务路由到 architect',\n    fallbackTargets: ['planner']\n  }),\n  createRule({\n    name: 'code-review',\n    target: 'code-reviewer',\n    trigger: ['review', 'check', 'validate', 'audit', 'inspect'],\n    priority: 70,\n    description: '代码审查任务路由到 code-reviewer',\n    fallbackTargets: ['planner']\n  }),\n  createRule({\n    name: 'refactor-clean',\n    target: 'refactor-cleaner',\n    trigger: ['refactor', 'restructure', 'cleanup', 'clean'],\n    priority: 65,\n    description: '重构任务路由到 refactor-cleaner',\n    fallbackTargets: ['planner']\n  }),\n  createRule({\n    name: 'build-fix',\n    target: 'build-error-resolver',\n    trigger: ['build', 'compile', 'error', 'broken'],\n    priority: 60,\n    description: '构建错误路由到 build-error-resolver',\n    fallbackTargets: ['planner']\n  }),\n  createRule({\n    name: 'doc-update',\n    target: 'doc-updater',\n    trigger: ['docs', 'documentation', 'readme', 'changelog'],\n    priority: 55,\n    description: '文档更新任务路由到 doc-updater',\n    fallbackTargets: ['planner']\n  }),\n  createRule({\n    name: 'e2e-test',\n    target: 'e2e-runner',\n    trigger: ['e2e', 'playwright', 'integration-test', 'end-to-end'],\n    priority: 75,\n    description: 'E2E 测试任务路由到 e2e-runner',\n    fallbackTargets: ['tdd-guide']\n  }),\n  createRule({\n    name: 'planner',\n    target: 'planner',\n    trigger: ['plan', 'quest', 'design', 'roadmap', 'epic'],\n    priority: 80,\n    description: '规划任务路由到 planner',\n    fallbackTargets: []\n  })\n];\n\n/**\n * @typedef {Object} Rule\n * @property {string} name - 规则唯一名称\n * @property {string} target - 路由目标\n * @property {string[]} trigger - 触发关键词\n * @property {number} priority - 优先级（0-100）\n * @property {string} matchMode - 匹配模式\n * @property {string[]} domains - 适用领域\n * @property {boolean} required - 是否强制执行\n * @property {string[]} fallbackTargets - 回退目标\n * @property {string} description - 描述\n */\n```\n\n**文件 2** -- CREATE `G:/project/auto-cli/tests/rule-engine.test.js`\n\n```javascript\n/**\n * RuleEngine 模块单元测试\n */\nimport { describe, it, expect, beforeEach } from 'vitest';\nimport {\n  createRule,\n  RuleEngine,\n  MatchMode,\n  DEFAULT_ROUTING_RULES\n} from '../src/router/rule-engine.js';\n\ndescribe('createRule', () => {\n  it('应该创建有效规则', () => {\n    const rule = createRule({\n      name: 'test-rule',\n      target: 'test-agent',\n      trigger: ['test', 'spec'],\n      priority: 80\n    });\n\n    expect(rule.name).toBe('test-rule');\n    expect(rule.target).toBe('test-agent');\n    expect(rule.trigger).toEqual(['test', 'spec']);\n    expect(rule.priority).toBe(80);\n    expect(rule.matchMode).toBe(MatchMode.ANY);\n    expect(rule.required).toBe(false);\n    expect(rule.fallbackTargets).toEqual([]);\n  });\n\n  it('应该使用默认值', () => {\n    const rule = createRule({\n      name: 'minimal',\n      target: 'agent'\n    });\n\n    expect(rule.priority).toBe(50);\n    expect(rule.trigger).toEqual([]);\n    expect(rule.matchMode).toBe(MatchMode.ANY);\n  });\n\n  it('应该在缺少必需字段时抛出异常', () => {\n    expect(() => createRule(null)).toThrow('Rule must have name and target');\n    expect(() => createRule({ name: 'no-target' })).toThrow('Rule must have name and target');\n    expect(() => createRule({ target: 'no-name' })).toThrow('Rule must have name and target');\n  });\n});\n\ndescribe('RuleEngine', () => {\n  let engine;\n\n  beforeEach(() => {\n    engine = new RuleEngine();\n  });\n\n  describe('register / unregister', () => {\n    it('应该注册和注销规则', () => {\n      const rule = createRule({\n        name: 'my-rule',\n        target: 'my-agent',\n        trigger: ['foo']\n      });\n\n      engine.register(rule);\n      expect(engine.size).toBe(1);\n\n      engine.unregister('my-rule');\n      expect(engine.size).toBe(0);\n    });\n\n    it('应该在注册重复名称时发出警告但不报错', () => {\n      const rule1 = createRule({ name: 'dup', target: 'a', trigger: ['x'] });\n      const rule2 = createRule({ name: 'dup', target: 'b', trigger: ['y'] });\n\n      engine.register(rule1);\n      engine.register(rule2); // 不抛异常\n      expect(engine.size).toBe(1);\n    });\n\n    it('registerBatch 应该批量注册', () => {\n      const rules = [\n        createRule({ name: 'r1', target: 'a', trigger: ['x'] }),\n        createRule({ name: 'r2', target: 'b', trigger: ['y'] })\n      ];\n\n      engine.registerBatch(rules);\n      expect(engine.size).toBe(2);\n    });\n  });\n\n  describe('match', () => {\n    it('应该根据关键词匹配规则', () => {\n      engine.register(\n        createRule({ name: 'security', target: 'sec-agent', trigger: ['auth', 'password'], priority: 100 })\n      );\n      engine.register(\n        createRule({ name: 'test', target: 'test-agent', trigger: ['test', 'spec'], priority: 80 })\n      );\n\n      const intent = {\n        keywords: ['fix', 'auth', 'login'],\n        domain: 'engineering',\n        type: 'debug',\n        securitySensitive: true\n      };\n\n      const matched = engine.match(intent);\n      expect(matched).toHaveLength(1);\n      expect(matched[0].name).toBe('security');\n    });\n\n    it('应该按优先级排序返回', () => {\n      engine.register(\n        createRule({ name: 'low', target: 'a', trigger: ['test'], priority: 30 })\n      );\n      engine.register(\n        createRule({ name: 'high', target: 'b', trigger: ['test'], priority: 90 })\n      );\n\n      const intent = { keywords: ['test', 'feature'], domain: 'engineering' };\n      const matched = engine.match(intent);\n\n      expect(matched).toHaveLength(2);\n      expect(matched[0].name).toBe('high');\n      expect(matched[1].name).toBe('low');\n    });\n\n    it('应该支持 ALL 匹配模式', () => {\n      engine.register(\n        createRule({\n          name: 'all-match',\n          target: 'agent',\n          trigger: ['test', 'coverage'],\n          matchMode: MatchMode.ALL,\n          priority: 50\n        })\n      );\n\n      // 只有 test\n      const intent1 = { keywords: ['test'], domain: 'testing' };\n      expect(engine.match(intent1)).toHaveLength(0);\n\n      // 同时有 test 和 coverage\n      const intent2 = { keywords: ['test', 'coverage'], domain: 'testing' };\n      expect(engine.match(intent2)).toHaveLength(1);\n    });\n\n    it('应该支持域过滤', () => {\n      engine.register(\n        createRule({\n          name: 'domain-rule',\n          target: 'agent',\n          trigger: ['data'],\n          domains: ['data', 'ml'],\n          priority: 50\n        })\n      );\n\n      // 匹配域\n      const intent1 = { keywords: ['data', 'analysis'], domain: 'data' };\n      expect(engine.match(intent1)).toHaveLength(1);\n\n      // 不匹配域\n      const intent2 = { keywords: ['data', 'analysis'], domain: 'engineering' };\n      expect(engine.match(intent2)).toHaveLength(0);\n    });\n\n    it('应该对空 intent 返回空列表', () => {\n      engine.register(\n        createRule({ name: 'r', target: 'a', trigger: ['x'], priority: 50 })\n      );\n      expect(engine.match(null)).toEqual([]);\n      expect(engine.match({})).toEqual([]);\n    });\n\n    it('应该跳过无触发词的规则', () => {\n      engine.register(\n        createRule({ name: 'no-trigger', target: 'a', trigger: [], priority: 50 })\n      );\n      const intent = { keywords: ['anything'], domain: 'engineering' };\n      expect(engine.match(intent)).toHaveLength(0);\n    });\n  });\n\n  describe('resolveConflict', () => {\n    it('应该对空候选返回 null', () => {\n      expect(engine.resolveConflict([], null)).toBeNull();\n      expect(engine.resolveConflict(null, null)).toBeNull();\n    });\n\n    it('应该对单个候选直接返回', () => {\n      const rule = createRule({ name: 'only', target: 'a', trigger: ['x'], priority: 50 });\n      expect(engine.resolveConflict([rule], null)).toBe(rule);\n    });\n\n    it('应该优先选择 required 规则', () => {\n      const normal = createRule({ name: 'normal', target: 'a', trigger: ['x'], priority: 90 });\n      const required = createRule({\n        name: 'required', target: 'b', trigger: ['x'], priority: 70, required: true\n      });\n\n      const result = engine.resolveConflict([normal, required], {});\n      expect(result.name).toBe('required');\n    });\n\n    it('应该在安全敏感时选择安全规则', () => {\n      const normal = createRule({ name: 'normal', target: 'a', trigger: ['code'], priority: 90 });\n      const security = createRule({\n        name: 'sec', target: 'sec-agent', trigger: ['auth'], priority: 60\n      });\n\n      const intent = { securitySensitive: true };\n      const result = engine.resolveConflict([normal, security], intent);\n      expect(result.name).toBe('sec');\n    });\n\n    it('应该默认选择最高优先级', () => {\n      const low = createRule({ name: 'low', target: 'a', trigger: ['x'], priority: 30 });\n      const high = createRule({ name: 'high', target: 'b', trigger: ['y'], priority: 90 });\n\n      const result = engine.resolveConflict([low, high], {});\n      expect(result.name).toBe('high');\n    });\n  });\n\n  describe('getFallbackChain', () => {\n    it('应该返回回退链', () => {\n      const rule = createRule({\n        name: 'main',\n        target: 'a',\n        trigger: ['x'],\n        fallbackTargets: ['backup1', 'backup2']\n      });\n\n      expect(engine.getFallbackChain(rule)).toEqual(['backup1', 'backup2']);\n    });\n\n    it('应该对无回退的规则返回空数组', () => {\n      const rule = createRule({ name: 'no-fallback', target: 'a', trigger: ['x'] });\n      expect(engine.getFallbackChain(rule)).toEqual([]);\n    });\n\n    it('应该对 null 输入返回空数组', () => {\n      expect(engine.getFallbackChain(null)).toEqual([]);\n    });\n  });\n\n  describe('listRules', () => {\n    it('应该按优先级降序列出规则', () => {\n      engine.register(createRule({ name: 'low', target: 'a', trigger: [], priority: 10 }));\n      engine.register(createRule({ name: 'high', target: 'b', trigger: [], priority: 90 }));\n      engine.register(createRule({ name: 'mid', target: 'c', trigger: [], priority: 50 }));\n\n      const list = engine.listRules();\n      expect(list[0].name).toBe('high');\n      expect(list[1].name).toBe('mid');\n      expect(list[2].name).toBe('low');\n    });\n  });\n});\n\ndescribe('DEFAULT_ROUTING_RULES', () => {\n  it('应该定义至少 7 条默认规则', () => {\n    expect(DEFAULT_ROUTING_RULES.length).toBeGreaterThanOrEqual(7);\n  });\n\n  it('每条规则应该有有效的 name、target、trigger', () => {\n    for (const rule of DEFAULT_ROUTING_RULES) {\n      expect(rule.name).toBeTruthy();\n      expect(rule.target).toBeTruthy();\n      expect(Array.isArray(rule.trigger)).toBe(true);\n      expect(rule.trigger.length).toBeGreaterThan(0);\n      expect(typeof rule.priority).toBe('number');\n    }\n  });\n\n  it('security-review 规则应该是 required', () => {\n    const sec = DEFAULT_ROUTING_RULES.find((r) => r.name === 'security-review');\n    expect(sec).toBeDefined();\n    expect(sec.required).toBe(true);\n    expect(sec.priority).toBe(100);\n  });\n});\n```\n\n**预判坑点**：\n1. `_matchRule` 方法中域过滤使用 `intent.domain`，这个字段由 IntentParser 的 `inferDomain` 生成。如果 domain 未在 SKILL_DOMAINS 中匹配到，会返回 `'integration'`（见 skill-types.js 第 112 行），所以规则中的 `domains` 数组需要使用正确的 domain id。\n2. `resolveConflict` 方法接收的 `candidates` 参数必须是已经按优先级排序的（因为 `match` 方法返回的就是排好序的）。如果外部直接传入未排序的数组，会取第一个而不是最高优先级。\n3. `DEFAULT_ROUTING_RULES` 中的 trigger 关键词要小写，因为 `_matchRule` 中对 keywords 和 triggers 都做了 `toLowerCase()` 处理。\n\n**验收标准**：\n| # | 验证点 | 验证命令 | 预期 |\n|---|-------|---------|------|\n| 1 | 文件存在 | `ls G:/project/auto-cli/src/router/rule-engine.js` | 文件存在 |\n| 2 | 测试通过 | `npx vitest run tests/rule-engine.test.js` | 全部通过 |\n| 3 | 默认规则数 | `node -e \"import('./src/router/rule-engine.js').then(m => console.log(m.DEFAULT_ROUTING_RULES.length))\"` | >= 7 |\n\n**回滚**：`rm G:/project/auto-cli/src/router/rule-engine.js G:/project/auto-cli/tests/rule-engine.test.js`\n\n---\n\n## Quest [1.3]：Agent 注册表 -- AgentRegistry\n\n**目标**：新建 `src/router/agent-registry.js`，管理所有可用 Agent 和 Skill 的能力描述、触发条件和元数据。\n\n**风险**：LOW 低（数据容器 + 查询，无副作用）\n\n**边界**：禁止修改已有文件；禁止引入新依赖\n\n**依赖**：无（独立模块）\n\n**合约**：产出 CONTRACT-3 (AgentEntry, SkillEntry)\n\n**完整实现**：\n\n**文件 1** -- CREATE `G:/project/auto-cli/src/router/agent-registry.js`\n\n```javascript\n/**\n * Agent 注册表\n * 管理所有可用 Agent 和 Skill 的注册、查询和能力匹配\n *\n * 设计来源: VIBE-SKILLS-ANALYSIS.md Agent Registry 架构\n */\nimport { logger } from '../logger.js';\n\n/**\n * Agent 能力级别\n */\nexport const CapabilityLevel = Object.freeze({\n  EXPERT: 'expert',     // 专家级\n  PROFICIENT: 'proficient', // 熟练\n  BASIC: 'basic'        // 基础\n});\n\n/**\n * Agent 类型\n */\nexport const AgentType = Object.freeze({\n  AGENT: 'agent',       // Agent（如 planner, architect）\n  SKILL: 'skill',       // Skill（如 security-review, tdd-workflow）\n  COMMAND: 'command'    // 命令（如 /auto:plan, /auto:tdd）\n});\n\n/**\n * 创建 Agent 注册条目\n * @param {Object} config\n * @param {string} config.name - 唯一名称\n * @param {string} config.displayName - 显示名称\n * @param {string} config.description - 描述\n * @param {string} config.type - 类型（AgentType 枚举值）\n * @param {string[]} config.capabilities - 能力标签列表\n * @param {string[]} config.triggerKeywords - 触发关键词\n * @param {number} [config.priority] - 默认优先级\n * @param {string} [config.domain] - 主领域\n * @param {string} [config.version] - 版本号\n * @returns {AgentEntry}\n */\nexport function createAgentEntry(config) {\n  if (!config || !config.name) {\n    throw new Error('AgentEntry must have a name');\n  }\n\n  return {\n    name: config.name,\n    displayName: config.displayName || config.name,\n    description: config.description || '',\n    type: config.type || AgentType.AGENT,\n    capabilities: config.capabilities || [],\n    triggerKeywords: config.triggerKeywords || [],\n    priority: config.priority ?? 50,\n    domain: config.domain || '',\n    version: config.version || '1.0.0'\n  };\n}\n\n/**\n * Agent 注册表类\n */\nexport class AgentRegistry {\n  constructor() {\n    /** @type {Map<string, AgentEntry>} */\n    this.entries = new Map();\n    /** @type {Map<string, Set<string>>} capability -> Set<entryName> */\n    this.capabilityIndex = new Map();\n  }\n\n  /**\n   * 注册 Agent/Skill/Command\n   * @param {AgentEntry} entry - 注册条目\n   */\n  register(entry) {\n    if (!entry || !entry.name) {\n      throw new Error('Entry must have a name');\n    }\n\n    this.entries.set(entry.name, entry);\n\n    // 更新能力索引\n    for (const cap of entry.capabilities) {\n      if (!this.capabilityIndex.has(cap)) {\n        this.capabilityIndex.set(cap, new Set());\n      }\n      this.capabilityIndex.get(cap).add(entry.name);\n    }\n\n    logger.debug(`Registry: registered ${entry.type} \"${entry.name}\"`);\n  }\n\n  /**\n   * 批量注册\n   * @param {AgentEntry[]} entries\n   */\n  registerBatch(entries) {\n    if (!Array.isArray(entries)) return;\n    for (const entry of entries) {\n      this.register(entry);\n    }\n  }\n\n  /**\n   * 注销\n   * @param {string} name - 名称\n   * @returns {boolean}\n   */\n  unregister(name) {\n    const entry = this.entries.get(name);\n    if (!entry) return false;\n\n    // 清除能力索引\n    for (const cap of entry.capabilities) {\n      const set = this.capabilityIndex.get(cap);\n      if (set) {\n        set.delete(name);\n        if (set.size === 0) {\n          this.capabilityIndex.delete(cap);\n        }\n      }\n    }\n\n    this.entries.delete(name);\n    return true;\n  }\n\n  /**\n   * 按名称查询\n   * @param {string} name - 名称\n   * @returns {AgentEntry|undefined}\n   */\n  get(name) {\n    return this.entries.get(name);\n  }\n\n  /**\n   * 检查是否已注册\n   * @param {string} name - 名称\n   * @returns {boolean}\n   */\n  has(name) {\n    return this.entries.has(name);\n  }\n\n  /**\n   * 按能力查询\n   * @param {string} capability - 能力标签\n   * @returns {AgentEntry[]}\n   */\n  findByCapability(capability) {\n    const names = this.capabilityIndex.get(capability) || new Set();\n    return Array.from(names)\n      .map((name) => this.entries.get(name))\n      .filter(Boolean)\n      .sort((a, b) => b.priority - a.priority);\n  }\n\n  /**\n   * 按类型查询\n   * @param {string} type - AgentType 枚举值\n   * @returns {AgentEntry[]}\n   */\n  findByType(type) {\n    return Array.from(this.entries.values())\n      .filter((e) => e.type === type)\n      .sort((a, b) => b.priority - a.priority);\n  }\n\n  /**\n   * 按触发关键词匹配\n   * @param {string[]} keywords - 用户意图关键词\n   * @returns {AgentEntry[]} 匹配的条目列表\n   */\n  findByKeywords(keywords) {\n    if (!keywords || keywords.length === 0) return [];\n\n    const lowerKeywords = keywords.map((k) => k.toLowerCase());\n    const scored = [];\n\n    for (const entry of this.entries.values()) {\n      const lowerTriggers = entry.triggerKeywords.map((t) => t.toLowerCase());\n      const matchCount = lowerTriggers.filter((t) => lowerKeywords.includes(t)).length;\n\n      if (matchCount > 0) {\n        scored.push({ entry, matchCount });\n      }\n    }\n\n    // 按匹配数 + 优先级排序\n    scored.sort((a, b) => {\n      if (b.matchCount !== a.matchCount) return b.matchCount - a.matchCount;\n      return b.entry.priority - a.entry.priority;\n    });\n\n    return scored.map((s) => s.entry);\n  }\n\n  /**\n   * 列出所有条目\n   * @returns {AgentEntry[]}\n   */\n  list() {\n    return Array.from(this.entries.values()).sort((a, b) => b.priority - a.priority);\n  }\n\n  /**\n   * 获取统计信息\n   * @returns {Object}\n   */\n  getStats() {\n    const byType = {};\n    for (const entry of this.entries.values()) {\n      byType[entry.type] = (byType[entry.type] || 0) + 1;\n    }\n\n    return {\n      total: this.entries.size,\n      byType,\n      totalCapabilities: this.capabilityIndex.size\n    };\n  }\n\n  /**\n   * 获取注册数量\n   * @returns {number}\n   */\n  get size() {\n    return this.entries.size;\n  }\n\n  /**\n   * 清空注册表\n   */\n  clear() {\n    this.entries.clear();\n    this.capabilityIndex.clear();\n  }\n}\n\n/**\n * 默认 Agent 注册表数据\n * 基于 auto-cli 的 11 个 Agent + 21 个 Skills + 15 个 Commands\n */\nexport const DEFAULT_AGENT_ENTRIES = [\n  // Agents (11)\n  createAgentEntry({\n    name: 'planner',\n    displayName: 'Planner',\n    description: '实现规划 - 复杂功能、重构',\n    type: AgentType.AGENT,\n    capabilities: ['planning', 'task-breakdown', 'roadmap'],\n    triggerKeywords: ['plan', '规划', '设计', 'breakdown', 'roadmap', 'quest'],\n    priority: 80,\n    domain: 'requirements'\n  }),\n  createAgentEntry({\n    name: 'architect',\n    displayName: 'Architect',\n    description: '系统设计 - 架构决策',\n    type: AgentType.AGENT,\n    capabilities: ['system-design', 'architecture', 'scalability'],\n    triggerKeywords: ['architecture', 'design', 'structure', 'system', '架构', '系统'],\n    priority: 85,\n    domain: 'engineering'\n  }),\n  createAgentEntry({\n    name: 'tdd-guide',\n    displayName: 'TDD Guide',\n    description: '测试驱动开发 - 新功能、Bug 修复',\n    type: AgentType.AGENT,\n    capabilities: ['tdd', 'testing', 'coverage'],\n    triggerKeywords: ['test', 'tdd', 'spec', 'coverage', '测试'],\n    priority: 90,\n    domain: 'testing'\n  }),\n  createAgentEntry({\n    name: 'code-reviewer',\n    displayName: 'Code Reviewer',\n    description: '代码审查 - 编写代码后',\n    type: AgentType.AGENT,\n    capabilities: ['code-review', 'quality', 'best-practices'],\n    triggerKeywords: ['review', '审查', 'check', 'validate'],\n    priority: 70,\n    domain: 'debugging'\n  }),\n  createAgentEntry({\n    name: 'security-reviewer',\n    displayName: 'Security Reviewer',\n    description: '安全分析 - 提交前',\n    type: AgentType.AGENT,\n    capabilities: ['security', 'vulnerability', 'audit'],\n    triggerKeywords: ['security', 'auth', 'password', 'sql', 'token', '安全'],\n    priority: 100,\n    domain: 'engineering'\n  }),\n  createAgentEntry({\n    name: 'build-error-resolver',\n    displayName: 'Build Error Resolver',\n    description: '修复构建错误 - 构建失败时',\n    type: AgentType.AGENT,\n    capabilities: ['build', 'compile', 'error-fix'],\n    triggerKeywords: ['build', 'compile', 'error', 'broken'],\n    priority: 60,\n    domain: 'debugging'\n  }),\n  createAgentEntry({\n    name: 'e2e-runner',\n    displayName: 'E2E Runner',\n    description: 'E2E 测试 - 关键用户流程',\n    type: AgentType.AGENT,\n    capabilities: ['e2e', 'playwright', 'integration'],\n    triggerKeywords: ['e2e', 'playwright', 'integration-test', 'end-to-end'],\n    priority: 75,\n    domain: 'testing'\n  }),\n  createAgentEntry({\n    name: 'refactor-cleaner',\n    displayName: 'Refactor Cleaner',\n    description: '死代码清理 - 代码维护',\n    type: AgentType.AGENT,\n    capabilities: ['refactor', 'cleanup', 'dead-code'],\n    triggerKeywords: ['refactor', 'cleanup', 'clean', '重构', '清理'],\n    priority: 65,\n    domain: 'debugging'\n  }),\n  createAgentEntry({\n    name: 'doc-updater',\n    displayName: 'Doc Updater',\n    description: '文档更新 - 更新文档',\n    type: AgentType.AGENT,\n    capabilities: ['documentation', 'readme', 'changelog'],\n    triggerKeywords: ['docs', 'documentation', 'readme', '文档'],\n    priority: 55,\n    domain: 'integration'\n  }),\n  createAgentEntry({\n    name: 'quest-designer',\n    displayName: 'Quest Designer',\n    description: '闯关大纲设计',\n    type: AgentType.AGENT,\n    capabilities: ['quest', 'blueprint', 'implementation-plan'],\n    triggerKeywords: ['quest', 'blueprint', '闯关'],\n    priority: 78,\n    domain: 'requirements'\n  }),\n  createAgentEntry({\n    name: 'multi-agent-orchestrator',\n    displayName: 'Multi-Agent Orchestrator',\n    description: '多 Agent 编排',\n    type: AgentType.AGENT,\n    capabilities: ['orchestration', 'multi-agent', 'pipeline'],\n    triggerKeywords: ['orchestrate', 'multi-agent', 'parallel', '编排'],\n    priority: 88,\n    domain: 'engineering'\n  })\n];\n\n/**\n * @typedef {Object} AgentEntry\n * @property {string} name - 唯一名称\n * @property {string} displayName - 显示名称\n * @property {string} description - 描述\n * @property {string} type - 类型（agent/skill/command）\n * @property {string[]} capabilities - 能力标签\n * @property {string[]} triggerKeywords - 触发关键词\n * @property {number} priority - 优先级\n * @property {string} domain - 主领域\n * @property {string} version - 版本号\n */\n```\n\n**文件 2** -- CREATE `G:/project/auto-cli/tests/agent-registry.test.js`\n\n```javascript\n/**\n * AgentRegistry 模块单元测试\n */\nimport { describe, it, expect, beforeEach } from 'vitest';\nimport {\n  createAgentEntry,\n  AgentRegistry,\n  AgentType,\n  CapabilityLevel,\n  DEFAULT_AGENT_ENTRIES\n} from '../src/router/agent-registry.js';\n\ndescribe('createAgentEntry', () => {\n  it('应该创建有效条目', () => {\n    const entry = createAgentEntry({\n      name: 'test-agent',\n      displayName: 'Test Agent',\n      description: 'For testing',\n      capabilities: ['testing'],\n      triggerKeywords: ['test', 'spec']\n    });\n\n    expect(entry.name).toBe('test-agent');\n    expect(entry.displayName).toBe('Test Agent');\n    expect(entry.type).toBe(AgentType.AGENT);\n    expect(entry.priority).toBe(50);\n  });\n\n  it('应该使用默认值', () => {\n    const entry = createAgentEntry({ name: 'minimal' });\n    expect(entry.displayName).toBe('minimal');\n    expect(entry.capabilities).toEqual([]);\n    expect(entry.triggerKeywords).toEqual([]);\n    expect(entry.version).toBe('1.0.0');\n  });\n\n  it('应该在缺少 name 时抛出异常', () => {\n    expect(() => createAgentEntry(null)).toThrow('AgentEntry must have a name');\n    expect(() => createAgentEntry({})).toThrow('AgentEntry must have a name');\n  });\n});\n\ndescribe('AgentRegistry', () => {\n  let registry;\n\n  beforeEach(() => {\n    registry = new AgentRegistry();\n  });\n\n  describe('register / unregister', () => {\n    it('应该注册和注销条目', () => {\n      const entry = createAgentEntry({\n        name: 'my-agent',\n        capabilities: ['cap-a']\n      });\n\n      registry.register(entry);\n      expect(registry.has('my-agent')).toBe(true);\n      expect(registry.size).toBe(1);\n\n      registry.unregister('my-agent');\n      expect(registry.has('my-agent')).toBe(false);\n      expect(registry.size).toBe(0);\n    });\n\n    it('注销时应该清理能力索引', () => {\n      const entry = createAgentEntry({\n        name: 'agent',\n        capabilities: ['cap-a', 'cap-b']\n      });\n\n      registry.register(entry);\n      registry.unregister('agent');\n\n      expect(registry.findByCapability('cap-a')).toEqual([]);\n      expect(registry.findByCapability('cap-b')).toEqual([]);\n    });\n\n    it('应该在 name 缺失时抛出异常', () => {\n      expect(() => registry.register(null)).toThrow('Entry must have a name');\n      expect(() => registry.register({})).toThrow('Entry must have a name');\n    });\n\n    it('registerBatch 应该批量注册', () => {\n      registry.registerBatch([\n        createAgentEntry({ name: 'a', capabilities: ['x'] }),\n        createAgentEntry({ name: 'b', capabilities: ['y'] })\n      ]);\n      expect(registry.size).toBe(2);\n    });\n\n    it('注销不存在的条目应返回 false', () => {\n      expect(registry.unregister('nonexistent')).toBe(false);\n    });\n  });\n\n  describe('findByCapability', () => {\n    it('应该按能力查找', () => {\n      registry.registerBatch([\n        createAgentEntry({ name: 'a', capabilities: ['testing'], priority: 80 }),\n        createAgentEntry({ name: 'b', capabilities: ['testing'], priority: 60 })\n      ]);\n\n      const results = registry.findByCapability('testing');\n      expect(results).toHaveLength(2);\n      expect(results[0].name).toBe('a'); // 高优先级在前\n    });\n\n    it('应该对不存在的能力返回空数组', () => {\n      expect(registry.findByCapability('nonexistent')).toEqual([]);\n    });\n  });\n\n  describe('findByType', () => {\n    it('应该按类型查找', () => {\n      registry.registerBatch([\n        createAgentEntry({ name: 'agent-1', type: AgentType.AGENT, priority: 50 }),\n        createAgentEntry({ name: 'skill-1', type: AgentType.SKILL, priority: 50 }),\n        createAgentEntry({ name: 'agent-2', type: AgentType.AGENT, priority: 80 })\n      ]);\n\n      const agents = registry.findByType(AgentType.AGENT);\n      expect(agents).toHaveLength(2);\n      expect(agents[0].name).toBe('agent-2'); // 高优先级在前\n    });\n  });\n\n  describe('findByKeywords', () => {\n    it('应该按关键词匹配并排序', () => {\n      registry.registerBatch([\n        createAgentEntry({ name: 'a', triggerKeywords: ['test', 'spec'], priority: 50 }),\n        createAgentEntry({ name: 'b', triggerKeywords: ['test'], priority: 80 }),\n        createAgentEntry({ name: 'c', triggerKeywords: ['review'], priority: 90 })\n      ]);\n\n      const results = registry.findByKeywords(['test', 'spec']);\n      // a 匹配 2 个，b 匹配 1 个，c 不匹配\n      expect(results).toHaveLength(2);\n      expect(results[0].name).toBe('a'); // 匹配数最多\n    });\n\n    it('应该对空关键词返回空数组', () => {\n      registry.register(createAgentEntry({ name: 'a', triggerKeywords: ['x'] }));\n      expect(registry.findByKeywords([])).toEqual([]);\n      expect(registry.findByKeywords(null)).toEqual([]);\n    });\n  });\n\n  describe('getStats', () => {\n    it('应该返回统计信息', () => {\n      registry.registerBatch([\n        createAgentEntry({ name: 'a', type: AgentType.AGENT, capabilities: ['x'] }),\n        createAgentEntry({ name: 'b', type: AgentType.SKILL, capabilities: ['x', 'y'] })\n      ]);\n\n      const stats = registry.getStats();\n      expect(stats.total).toBe(2);\n      expect(stats.byType[AgentType.AGENT]).toBe(1);\n      expect(stats.byType[AgentType.SKILL]).toBe(1);\n      expect(stats.totalCapabilities).toBe(2);\n    });\n  });\n\n  describe('clear', () => {\n    it('应该清空所有数据', () => {\n      registry.register(createAgentEntry({ name: 'a', capabilities: ['x'] }));\n      registry.clear();\n      expect(registry.size).toBe(0);\n    });\n  });\n});\n\ndescribe('DEFAULT_AGENT_ENTRIES', () => {\n  it('应该包含 11 个 Agent', () => {\n    expect(DEFAULT_AGENT_ENTRIES.length).toBe(11);\n  });\n\n  it('所有条目应该是 AGENT 类型', () => {\n    for (const entry of DEFAULT_AGENT_ENTRIES) {\n      expect(entry.type).toBe(AgentType.AGENT);\n    }\n  });\n\n  it('每个 Agent 应该有 capabilities 和 triggerKeywords', () => {\n    for (const entry of DEFAULT_AGENT_ENTRIES) {\n      expect(entry.capabilities.length).toBeGreaterThan(0);\n      expect(entry.triggerKeywords.length).toBeGreaterThan(0);\n    }\n  });\n\n  it('应该包含核心 Agent', () => {\n    const names = DEFAULT_AGENT_ENTRIES.map((e) => e.name);\n    expect(names).toContain('planner');\n    expect(names).toContain('architect');\n    expect(names).toContain('tdd-guide');\n    expect(names).toContain('security-reviewer');\n    expect(names).toContain('code-reviewer');\n  });\n});\n```\n\n**预判坑点**：\n1. `unregister` 方法需要清理 `capabilityIndex`，否则会残留引用。已处理。\n2. `findByKeywords` 的排序逻辑是先按匹配数、再按优先级。如果两个条目匹配数相同但优先级不同，高优先级的排前面。\n3. `DEFAULT_AGENT_ENTRIES` 的 `priority` 值与 `DEFAULT_ROUTING_RULES` 中一致（如 security-reviewer = 100, tdd-guide = 90），但这两个是独立系统，Router 最终使用的是 Rule 的 priority。\n\n**验收标准**：\n| # | 验证点 | 验证命令 | 预期 |\n|---|-------|---------|------|\n| 1 | 文件存在 | `ls G:/project/auto-cli/src/router/agent-registry.js` | 文件存在 |\n| 2 | 测试通过 | `npx vitest run tests/agent-registry.test.js` | 全部通过 |\n| 3 | 默认 Agent 数量 | `node -e \"import('./src/router/agent-registry.js').then(m => console.log(m.DEFAULT_AGENT_ENTRIES.length))\"` | 11 |\n\n**回滚**：`rm G:/project/auto-cli/src/router/agent-registry.js G:/project/auto-cli/tests/agent-registry.test.js`\n\n---\n\n## Quest [1.4]：Canonical Router 主路由器\n\n**目标**：新建 `src/router/canonical-router.js`，整合 IntentParser + RuleEngine + AgentRegistry，实现完整的路由决策流程。\n\n**风险**：MEDIUM 中（串联三个模块，需要确保合约一致性）\n\n**边界**：禁止修改已有文件；禁止引入新依赖；禁止在 Router 中实现业务逻辑（路由只做调度决策）\n\n**依赖**：Quest 1.1 + Quest 1.2 + Quest 1.3\n\n**合约**：产出 CONTRACT-4 (RoutingResult), 消费 CONTRACT-1/2/3\n\n**完整实现**：\n\n**文件 1** -- CREATE `G:/project/auto-cli/src/router/canonical-router.js`\n\n```javascript\n/**\n * Canonical Router（权威路由器）\n * 单一真理源的路由决策中心\n *\n * 职责：\n * - 整合 IntentParser、RuleEngine、AgentRegistry\n * - 提供唯一的路由决策入口\n * - 支持回退策略和路由日志\n *\n * 架构原则：\n * - 所有路由决策必须经过此 Router\n * - 禁止 Agent/Skill 之间直接调用\n * - 路由结果包含完整的决策链路（可解释性）\n */\nimport { parseIntent } from './intent-parser.js';\nimport { RuleEngine, DEFAULT_ROUTING_RULES } from './rule-engine.js';\nimport { AgentRegistry, DEFAULT_AGENT_ENTRIES } from './agent-registry.js';\nimport { logger } from '../logger.js';\n\n/**\n * 路由结果状态\n */\nexport const RouteStatus = Object.freeze({\n  SUCCESS: 'success',\n  FALLBACK: 'fallback',\n  NO_MATCH: 'no_match',\n  ERROR: 'error'\n});\n\n/**\n * Canonical Router 类\n * 唯一的路由决策中心\n */\nexport class CanonicalRouter {\n  /**\n   * @param {Object} [options] - 配置选项\n   * @param {RuleEngine} [options.ruleEngine] - 自定义规则引擎\n   * @param {AgentRegistry} [options.agentRegistry] - 自定义 Agent 注册表\n   * @param {boolean} [options.useDefaults] - 是否加载默认规则和 Agent（默认 true）\n   */\n  constructor(options = {}) {\n    this.ruleEngine = options.ruleEngine || new RuleEngine();\n    this.agentRegistry = options.agentRegistry || new AgentRegistry();\n    this.routingHistory = [];\n    this.maxHistorySize = 100;\n\n    // 加载默认数据\n    if (options.useDefaults !== false) {\n      this.ruleEngine.registerBatch(DEFAULT_ROUTING_RULES);\n      this.agentRegistry.registerBatch(DEFAULT_AGENT_ENTRIES);\n    }\n\n    logger.info('CanonicalRouter initialized');\n  }\n\n  /**\n   * 路由决策（核心方法 -- 唯一入口）\n   * @param {string} userInput - 用户输入\n   * @param {Object} [context] - 上下文\n   * @param {number} [context.fileCount] - 涉及文件数\n   * @param {string[]} [context.dependencies] - 依赖列表\n   * @returns {RoutingResult}\n   */\n  route(userInput, context = {}) {\n    const startTime = Date.now();\n\n    try {\n      // 步骤 1: 意图解析\n      const intent = parseIntent(userInput, context);\n\n      // 步骤 2: 规则匹配\n      const matchedRules = this.ruleEngine.match(intent);\n\n      // 步骤 3: 冲突解决\n      const selectedRule = this.ruleEngine.resolveConflict(matchedRules, intent);\n\n      // 步骤 4: 查找目标 Agent\n      let targetAgent = null;\n      let status = RouteStatus.NO_MATCH;\n\n      if (selectedRule) {\n        targetAgent = this.agentRegistry.get(selectedRule.target);\n\n        if (targetAgent) {\n          status = RouteStatus.SUCCESS;\n        } else {\n          // 目标 Agent 未注册，尝试回退链\n          const fallbackChain = this.ruleEngine.getFallbackChain(selectedRule);\n          for (const fallbackName of fallbackChain) {\n            const fallbackAgent = this.agentRegistry.get(fallbackName);\n            if (fallbackAgent) {\n              targetAgent = fallbackAgent;\n              status = RouteStatus.FALLBACK;\n              break;\n            }\n          }\n        }\n      }\n\n      // 步骤 4b: 如果规则无匹配，尝试按关键词在 Registry 中查找\n      if (!targetAgent && intent.keywords.length > 0) {\n        const keywordMatches = this.agentRegistry.findByKeywords(intent.keywords);\n        if (keywordMatches.length > 0) {\n          targetAgent = keywordMatches[0];\n          status = RouteStatus.FALLBACK;\n        }\n      }\n\n      const duration = Date.now() - startTime;\n\n      const result = {\n        status,\n        intent,\n        matchedRules,\n        selectedRule: selectedRule || null,\n        target: targetAgent ? {\n          name: targetAgent.name,\n          displayName: targetAgent.displayName,\n          type: targetAgent.type,\n          capabilities: targetAgent.capabilities\n        } : null,\n        fallbackChain: selectedRule ? this.ruleEngine.getFallbackChain(selectedRule) : [],\n        duration,\n        timestamp: new Date().toISOString()\n      };\n\n      // 记录路由历史\n      this._recordRouting(result);\n\n      logger.info(`Route: \"${userInput}\" -> ${targetAgent ? targetAgent.name : 'NONE'} (${status}, ${duration}ms)`);\n\n      return result;\n    } catch (error) {\n      const duration = Date.now() - startTime;\n      logger.error(`Route error: ${error.message}`);\n\n      return {\n        status: RouteStatus.ERROR,\n        intent: { raw: userInput, keywords: [], type: 'general', domain: '', complexity: 'LOW', complexityScore: 0, recommendedAgents: [], securitySensitive: false },\n        matchedRules: [],\n        selectedRule: null,\n        target: null,\n        fallbackChain: [],\n        duration,\n        timestamp: new Date().toISOString(),\n        error: error.message\n      };\n    }\n  }\n\n  /**\n   * 获取路由历史\n   * @param {number} [limit] - 返回条数\n   * @returns {RoutingResult[]}\n   */\n  getHistory(limit = 10) {\n    return this.routingHistory.slice(-limit);\n  }\n\n  /**\n   * 清空路由历史\n   */\n  clearHistory() {\n    this.routingHistory = [];\n  }\n\n  /**\n   * 获取路由统计\n   * @returns {Object}\n   */\n  getStats() {\n    const total = this.routingHistory.length;\n    const byStatus = {};\n    let totalDuration = 0;\n\n    for (const record of this.routingHistory) {\n      byStatus[record.status] = (byStatus[record.status] || 0) + 1;\n      totalDuration += record.duration || 0;\n    }\n\n    return {\n      total,\n      byStatus,\n      avgDuration: total > 0 ? Math.round(totalDuration / total) : 0,\n      registryStats: this.agentRegistry.getStats(),\n      ruleCount: this.ruleEngine.size\n    };\n  }\n\n  /**\n   * 获取推荐的路由方案（用于展示建议）\n   * @param {string} userInput - 用户输入\n   * @param {Object} [context] - 上下文\n   * @returns {Object} 包含所有候选方案的推荐结果\n   */\n  getRecommendations(userInput, context = {}) {\n    const intent = parseIntent(userInput, context);\n    const matchedRules = this.ruleEngine.match(intent);\n    const keywordMatches = this.agentRegistry.findByKeywords(intent.keywords);\n\n    return {\n      intent,\n      rules: matchedRules.map((r) => ({\n        name: r.name,\n        target: r.target,\n        priority: r.priority,\n        description: r.description\n      })),\n      agents: keywordMatches.map((a) => ({\n        name: a.name,\n        displayName: a.displayName,\n        capabilities: a.capabilities,\n        priority: a.priority\n      })),\n      recommendedAgents: intent.recommendedAgents\n    };\n  }\n\n  /**\n   * 记录路由历史\n   * @private\n   * @param {RoutingResult} result\n   */\n  _recordRouting(result) {\n    this.routingHistory.push(result);\n    if (this.routingHistory.length > this.maxHistorySize) {\n      this.routingHistory.shift();\n    }\n  }\n}\n\n/**\n * 创建预配置的 Canonical Router 实例\n * @param {Object} [options] - 配置选项\n * @returns {CanonicalRouter}\n */\nexport function createRouter(options = {}) {\n  return new CanonicalRouter(options);\n}\n\n/**\n * @typedef {Object} RoutingResult\n * @property {string} status - 路由状态（RouteStatus 枚举值）\n * @property {import('./intent-parser.js').IntentResult} intent - 意图解析结果\n * @property {import('./rule-engine.js').Rule[]} matchedRules - 匹配的规则列表\n * @property {import('./rule-engine.js').Rule|null} selectedRule - 选中的规则\n * @property {{ name: string, displayName: string, type: string, capabilities: string[] }|null} target - 路由目标\n * @property {string[]} fallbackChain - 回退链\n * @property {number} duration - 耗时（ms）\n * @property {string} timestamp - 时间戳\n * @property {string} [error] - 错误信息\n */\n```\n\n**文件 2** -- CREATE `G:/project/auto-cli/tests/canonical-router.test.js`\n\n```javascript\n/**\n * CanonicalRouter 模块单元测试\n */\nimport { describe, it, expect, beforeEach } from 'vitest';\nimport {\n  CanonicalRouter,\n  RouteStatus,\n  createRouter\n} from '../src/router/canonical-router.js';\nimport { RuleEngine, createRule } from '../src/router/rule-engine.js';\nimport { AgentRegistry, createAgentEntry } from '../src/router/agent-registry.js';\n\ndescribe('CanonicalRouter', () => {\n  let router;\n\n  beforeEach(() => {\n    router = new CanonicalRouter();\n  });\n\n  describe('route', () => {\n    it('应该将安全相关请求路由到 security-reviewer', () => {\n      const result = router.route('fix auth token vulnerability');\n\n      expect(result.status).toBe(RouteStatus.SUCCESS);\n      expect(result.target).toBeDefined();\n      expect(result.target.name).toBe('security-reviewer');\n      expect(result.intent.securitySensitive).toBe(true);\n      expect(result.selectedRule).toBeDefined();\n      expect(result.duration).toBeGreaterThanOrEqual(0);\n    });\n\n    it('应该将测试请求路由到 tdd-guide', () => {\n      const result = router.route('write unit tests for calculator');\n\n      expect(result.status).toBe(RouteStatus.SUCCESS);\n      expect(result.target.name).toBe('tdd-guide');\n    });\n\n    it('应该将架构请求路由到 architect', () => {\n      const result = router.route('design system architecture for microservices');\n\n      expect(result.status).toBe(RouteStatus.SUCCESS);\n      expect(result.target.name).toBe('architect');\n    });\n\n    it('应该将审查请求路由到 code-reviewer', () => {\n      const result = router.route('review the pull request');\n\n      expect(result.status).toBe(RouteStatus.SUCCESS);\n      expect(result.target.name).toBe('code-reviewer');\n    });\n\n    it('应该对无法匹配的输入返回 NO_MATCH 或 FALLBACK', () => {\n      const result = router.route('random gibberish xyz');\n\n      expect([RouteStatus.NO_MATCH, RouteStatus.FALLBACK]).toContain(result.status);\n    });\n\n    it('应该返回完整的 IntentResult', () => {\n      const result = router.route('fix the login bug');\n\n      expect(result.intent).toBeDefined();\n      expect(result.intent.raw).toBe('fix the login bug');\n      expect(result.intent.keywords.length).toBeGreaterThan(0);\n      expect(result.intent.type).toBeDefined();\n    });\n\n    it('应该处理空输入', () => {\n      const result = router.route('');\n\n      expect(result).toBeDefined();\n      expect(result.status).toBeDefined();\n      expect(result.intent).toBeDefined();\n    });\n\n    it('应该记录路由历史', () => {\n      router.route('test something');\n      router.route('review code');\n\n      const history = router.getHistory();\n      expect(history).toHaveLength(2);\n    });\n\n    it('应该处理带有 context 的请求', () => {\n      const result = router.route('refactor the authentication system', {\n        fileCount: 10,\n        dependencies: ['database', 'api']\n      });\n\n      expect(result).toBeDefined();\n      expect(result.intent.complexity).toBeDefined();\n    });\n  });\n\n  describe('fallback', () => {\n    it('应该在目标 Agent 不存在时使用回退链', () => {\n      // 使用自定义规则和注册表\n      const ruleEngine = new RuleEngine();\n      const agentRegistry = new AgentRegistry();\n\n      ruleEngine.register(createRule({\n        name: 'test-fallback',\n        target: 'nonexistent-agent',\n        trigger: ['trigger-word'],\n        priority: 90,\n        fallbackTargets: ['fallback-agent']\n      }));\n\n      agentRegistry.register(createAgentEntry({\n        name: 'fallback-agent',\n        displayName: 'Fallback Agent',\n        triggerKeywords: ['trigger-word']\n      }));\n\n      const customRouter = new CanonicalRouter({\n        ruleEngine,\n        agentRegistry,\n        useDefaults: false\n      });\n\n      const result = customRouter.route('trigger-word task');\n\n      expect(result.status).toBe(RouteStatus.FALLBACK);\n      expect(result.target.name).toBe('fallback-agent');\n    });\n  });\n\n  describe('error handling', () => {\n    it('应该在异常时返回 ERROR 状态', () => {\n      // 强制让 parseIntent 抛出异常是不太容易的，\n      // 但 route 方法内部有 try-catch，不应抛出\n      const result = router.route('normal request');\n      expect(result.status).not.toBe(RouteStatus.ERROR);\n    });\n  });\n\n  describe('getRecommendations', () => {\n    it('应该返回推荐方案', () => {\n      const recs = router.getRecommendations('test and review the code');\n\n      expect(recs.intent).toBeDefined();\n      expect(recs.rules).toBeDefined();\n      expect(Array.isArray(recs.rules)).toBe(true);\n      expect(recs.agents).toBeDefined();\n      expect(Array.isArray(recs.agents)).toBe(true);\n      expect(recs.recommendedAgents).toBeDefined();\n    });\n  });\n\n  describe('getStats', () => {\n    it('应该返回路由统计', () => {\n      router.route('test');\n      router.route('review');\n      router.route('build error');\n\n      const stats = router.getStats();\n\n      expect(stats.total).toBe(3);\n      expect(stats.byStatus).toBeDefined();\n      expect(stats.registryStats).toBeDefined();\n      expect(stats.ruleCount).toBeGreaterThan(0);\n      expect(stats.avgDuration).toBeGreaterThanOrEqual(0);\n    });\n  });\n\n  describe('history management', () => {\n    it('应该限制历史大小', () => {\n      router.maxHistorySize = 5;\n\n      for (let i = 0; i < 10; i++) {\n        router.route(`task ${i}`);\n      }\n\n      expect(router.getHistory()).toHaveLength(5);\n    });\n\n    it('clearHistory 应该清空历史', () => {\n      router.route('test');\n      router.clearHistory();\n      expect(router.getHistory()).toHaveLength(0);\n    });\n  });\n});\n\ndescribe('createRouter', () => {\n  it('应该创建默认配置的 Router', () => {\n    const r = createRouter();\n    expect(r).toBeInstanceOf(CanonicalRouter);\n    expect(r.ruleEngine.size).toBeGreaterThan(0);\n    expect(r.agentRegistry.size).toBeGreaterThan(0);\n  });\n\n  it('应该创建空配置的 Router', () => {\n    const r = createRouter({ useDefaults: false });\n    expect(r.ruleEngine.size).toBe(0);\n    expect(r.agentRegistry.size).toBe(0);\n  });\n});\n```\n\n**预判坑点**：\n1. `route` 方法中当 `selectedRule` 匹配但 `targetAgent` 未在 Registry 注册时，会尝试回退链。如果回退链中也没有注册的 Agent，则最终会尝试关键词匹配 Registry。这个三层降级逻辑需要测试覆盖。\n2. `routingHistory` 使用 `shift()` 来保持大小限制，这在 `maxHistorySize` 很大时性能不佳。当前默认 100 条不会有问题。\n3. `result.target` 是一个精简对象（只含 name/displayName/type/capabilities），不是完整的 AgentEntry。这是故意的 -- 路由结果不应该暴露内部实现细节。\n\n反模式警告：\n- 不要在 Router 中实现 Agent 的执行逻辑（Router 只做决策，不做执行）\n- 不要让 Router 依赖具体的 Agent 实现类型\n- 不要跳过 RuleEngine 直接在 Router 中硬编码路由逻辑\n\n**验收标准**：\n| # | 验证点 | 验证命令 | 预期 |\n|---|-------|---------|------|\n| 1 | 文件存在 | `ls G:/project/auto-cli/src/router/canonical-router.js` | 文件存在 |\n| 2 | 测试通过 | `npx vitest run tests/canonical-router.test.js` | 全部通过 |\n| 3 | 路由正确性 | `node -e \"import('./src/router/canonical-router.js').then(m => { const r = new m.CanonicalRouter(); const res = r.route('fix auth bug'); console.log(res.target.name, res.status) })\"` | security-reviewer success |\n\n**回滚**：`rm G:/project/auto-cli/src/router/canonical-router.js G:/project/auto-cli/tests/canonical-router.test.js`\n\n---\n\n## Quest [1.5]：Router 模块导出 + CLI 集成\n\n**目标**：创建 `src/router/index.js` 统一导出，在 `bin/cli.js` 中注册 `auto route` 命令，在 `src/index.js` 中导出 `runRoute` 函数。\n\n**风险**：MEDIUM 中（修改已有文件，需要精确锚点插入）\n\n**边界**：禁止修改 `src/router/` 下 Quest 1.1-1.4 创建的文件；禁止修改非锚点区域的代码\n\n**依赖**：Quest 1.1 + 1.2 + 1.3 + 1.4\n\n**合约**：消费 CONTRACT-4 (RoutingResult)\n\n**完整实现**：\n\n**文件 1** -- CREATE `G:/project/auto-cli/src/router/index.js`\n\n```javascript\n/**\n * Router 模块统一导出\n * 所有路由相关组件的入口文件\n */\n\n// 意图解析器\nexport {\n  parseIntent,\n  extractKeywords,\n  detectIntentType,\n  assessComplexity,\n  checkSecuritySensitivity,\n  IntentType,\n  ComplexityLevel\n} from './intent-parser.js';\n\n// 规则引擎\nexport {\n  createRule,\n  RuleEngine,\n  MatchMode,\n  DEFAULT_ROUTING_RULES\n} from './rule-engine.js';\n\n// Agent 注册表\nexport {\n  createAgentEntry,\n  AgentRegistry,\n  AgentType,\n  CapabilityLevel,\n  DEFAULT_AGENT_ENTRIES\n} from './agent-registry.js';\n\n// Canonical Router\nexport {\n  CanonicalRouter,\n  RouteStatus,\n  createRouter\n} from './canonical-router.js';\n```\n\n**文件 2** -- MODIFY `G:/project/auto-cli/src/index.js`\n\n插入锚点：在 `import { SkillInstaller } from './skills/skill-installer.js';` 行之后插入\n\n```javascript\nimport { CanonicalRouter, RouteStatus } from './router/index.js';\n```\n\n插入锚点：在文件末尾（`export async function runSkillsInstall` 函数的最后一个 `}` 之后）插入\n\n```javascript\n\n/**\n * 路由决策 - 分析用户意图并推荐最佳 Agent\n */\nexport async function runRoute(userInput, options = {}) {\n  const router = new CanonicalRouter();\n\n  if (options.recommendations) {\n    // 展示推荐方案模式\n    const recs = router.getRecommendations(userInput);\n\n    console.log('');\n    console.log(chalk.cyan.bold('路由推荐方案'));\n    console.log('');\n    console.log(chalk.white('意图分析:'));\n    console.log(chalk.gray(`  类型: ${recs.intent.type}`));\n    console.log(chalk.gray(`  领域: ${recs.intent.domain}`));\n    console.log(chalk.gray(`  复杂度: ${recs.intent.complexity} (${recs.intent.complexityScore}分)`));\n    console.log(chalk.gray(`  安全敏感: ${recs.intent.securitySensitive ? '是' : '否'}`));\n    console.log(chalk.gray(`  关键词: ${recs.intent.keywords.join(', ')}`));\n    console.log('');\n\n    if (recs.recommendedAgents.length > 0) {\n      console.log(chalk.white('推荐 Agent (按复杂度):'));\n      for (const agent of recs.recommendedAgents) {\n        console.log(chalk.gray(`  - ${agent}`));\n      }\n      console.log('');\n    }\n\n    if (recs.rules.length > 0) {\n      console.log(chalk.white('匹配的规则 (按优先级):'));\n      for (const rule of recs.rules) {\n        console.log(chalk.green(`  ${rule.name.padEnd(20)} priority:${rule.priority} -> ${rule.target}`));\n        if (rule.description) {\n          console.log(chalk.gray(`    ${rule.description}`));\n        }\n      }\n      console.log('');\n    }\n\n    if (recs.agents.length > 0) {\n      console.log(chalk.white('关键词匹配的 Agent:'));\n      for (const agent of recs.agents) {\n        console.log(chalk.green(`  ${agent.name.padEnd(20)} [${agent.capabilities.join(', ')}]`));\n      }\n      console.log('');\n    }\n\n    return;\n  }\n\n  // 执行路由\n  const result = router.route(userInput, {\n    fileCount: options.fileCount ? Number(options.fileCount) : undefined,\n    dependencies: options.dependencies ? options.dependencies.split(',').map((s) => s.trim()) : undefined\n  });\n\n  console.log('');\n  console.log(chalk.cyan.bold('路由结果'));\n  console.log('');\n\n  // 意图信息\n  console.log(chalk.white('意图分析:'));\n  console.log(chalk.gray(`  类型: ${result.intent.type}`));\n  console.log(chalk.gray(`  领域: ${result.intent.domain}`));\n  console.log(chalk.gray(`  复杂度: ${result.intent.complexity} (${result.intent.complexityScore}分)`));\n  console.log(chalk.gray(`  安全敏感: ${result.intent.securitySensitive ? '是' : '否'}`));\n  console.log('');\n\n  // 路由结果\n  if (result.target) {\n    const statusIcon = result.status === RouteStatus.SUCCESS ? chalk.green('[SUCCESS]') : chalk.yellow('[FALLBACK]');\n    console.log(`${statusIcon} ${chalk.white.bold(result.target.displayName)}`);\n    console.log(chalk.gray(`  名称: ${result.target.name}`));\n    console.log(chalk.gray(`  类型: ${result.target.type}`));\n    console.log(chalk.gray(`  能力: ${result.target.capabilities.join(', ')}`));\n    console.log(chalk.gray(`  耗时: ${result.duration}ms`));\n  } else {\n    console.log(chalk.yellow('[NO_MATCH] 未找到匹配的 Agent'));\n    if (result.intent.recommendedAgents.length > 0) {\n      console.log(chalk.gray(`  建议: ${result.intent.recommendedAgents.join(', ')}`));\n    }\n  }\n\n  // 匹配规则\n  if (result.matchedRules.length > 0 && options.verbose) {\n    console.log('');\n    console.log(chalk.white('匹配规则:'));\n    for (const rule of result.matchedRules) {\n      const marker = rule === result.selectedRule ? chalk.green(' *') : '  ';\n      console.log(`${marker} ${rule.name} (priority:${rule.priority}) -> ${rule.target}`);\n    }\n  }\n\n  // 回退链\n  if (result.fallbackChain.length > 0 && options.verbose) {\n    console.log('');\n    console.log(chalk.white('回退链:'));\n    console.log(chalk.gray(`  ${result.fallbackChain.join(' -> ')}`));\n  }\n\n  // JSON 输出\n  if (options.json) {\n    console.log('');\n    console.log(JSON.stringify(result, null, 2));\n  }\n\n  console.log('');\n}\n```\n\n**文件 3** -- MODIFY `G:/project/auto-cli/bin/cli.js`\n\n插入锚点：在 `import {` 块中（`runSkillsInstall` 导入行之后，即 `'../src/index.js';` 之前那行），找到以下文本：\n\n```\n  runSkillsInstall\n} from '../src/index.js';\n```\n\n替换为：\n\n```\n  runSkillsInstall,\n  runRoute\n} from '../src/index.js';\n```\n\n插入锚点：在 `program.parse();` 行之前插入\n\n```javascript\n// 路由命令\nprogram\n  .command('route <intent>')\n  .description('智能路由决策 - 分析意图并推荐最佳 Agent')\n  .option('-r, --recommendations', '展示推荐方案（不执行路由）')\n  .option('-v, --verbose', '显示详细信息（匹配规则、回退链）')\n  .option('--json', '以 JSON 格式输出路由结果')\n  .option('--file-count <n>', '涉及文件数量')\n  .option('--dependencies <list>', '依赖列表，逗号分隔')\n  .action(async (intent, options) => {\n    try {\n      await runRoute(intent, options);\n    } catch (error) {\n      console.error(chalk.red('错误：'), error.message);\n      process.exit(1);\n    }\n  });\n\n```\n\n**文件 4** -- MODIFY `G:/project/auto-cli/package.json`\n\n插入锚点：在 `\"version\": \"0.10.0\"` 行替换为\n\n```\n  \"version\": \"0.11.0\",\n```\n\n**预判坑点**：\n1. `bin/cli.js` 中的 import 列表使用 `{ ... } from '../src/index.js'` 格式，新增的 `runRoute` 必须加在 `runSkillsInstall` 后面且在闭合大括号 `}` 之前，否则导入语法错误。\n2. `src/index.js` 中已经导入了 `chalk`，`runRoute` 函数中可以直接使用 `chalk`，不需要额外导入。\n3. `CanonicalRouter` 和 `RouteStatus` 是从 `./router/index.js` 导入的，不是从各个子模块分别导入。确保 `src/router/index.js` 先创建再修改 `src/index.js`。\n\n反模式警告：\n- 不要在 `runRoute` 中实现路由逻辑，只做 CLI 展示和参数传递\n- 不要修改 `bin/cli.js` 中已有的命令定义\n- `package.json` 版本号只改 version 字段，不动其他字段\n\n**验收标准**：\n| # | 验证点 | 验证命令 | 预期 |\n|---|-------|---------|------|\n| 1 | 模块导出文件存在 | `ls G:/project/auto-cli/src/router/index.js` | 文件存在 |\n| 2 | 全量编译测试 | `npx vitest run` | 全部通过（含新增 + 原有） |\n| 3 | CLI 命令注册 | `node bin/cli.js route --help` | 显示 route 命令帮助 |\n| 4 | 路由功能 | `node bin/cli.js route \"fix auth bug\"` | 输出 security-reviewer |\n| 5 | 推荐模式 | `node bin/cli.js route \"test and review\" --recommendations` | 显示多规则推荐 |\n| 6 | JSON 输出 | `node bin/cli.js route \"build error\" --json` | 有效 JSON |\n\n**回滚**：\n```bash\nrm G:/project/auto-cli/src/router/index.js\ngit checkout -- G:/project/auto-cli/src/index.js G:/project/auto-cli/bin/cli.js G:/project/auto-cli/package.json\n```\n\n---\n\n## 合约一致性校验结果\n\n| 合约 | 产出方 | 消费方 | 字段完整性 | 类型一致性 | 状态 |\n|------|--------|--------|-----------|-----------|------|\n| CONTRACT-1 IntentResult | Quest 1.1 | Quest 1.2, 1.4 | raw, keywords, type, domain, complexity, securitySensitive, recommendedAgents | keywords:string[], type:string, complexity:string | PASS |\n| CONTRACT-2 Rule | Quest 1.2 | Quest 1.4 | name, target, trigger, priority, matchMode, domains, required, fallbackTargets | trigger:string[], priority:number | PASS |\n| CONTRACT-3 AgentEntry | Quest 1.3 | Quest 1.4 | name, displayName, description, type, capabilities, triggerKeywords, priority | capabilities:string[], type:string | PASS |\n| CONTRACT-4 RoutingResult | Quest 1.4 | Quest 1.5 | status, intent, matchedRules, selectedRule, target, fallbackChain, duration | status:string, target:{name,...}|null | PASS |\n\n**路径校验**：\n\n| 文件路径 | 操作 | 验证 |\n|---------|------|------|\n| `G:/project/auto-cli/src/router/intent-parser.js` | CREATE | 尚不存在（正确） |\n| `G:/project/auto-cli/src/router/rule-engine.js` | CREATE | 尚不存在（正确） |\n| `G:/project/auto-cli/src/router/agent-registry.js` | CREATE | 尚不存在（正确） |\n| `G:/project/auto-cli/src/router/canonical-router.js` | CREATE | 尚不存在（正确） |\n| `G:/project/auto-cli/src/router/index.js` | CREATE | 尚不存在（正确） |\n| `G:/project/auto-cli/src/index.js` | MODIFY | 已存在 (PASS) |\n| `G:/project/auto-cli/bin/cli.js` | MODIFY | 已存在 (PASS) |\n| `G:/project/auto-cli/package.json` | MODIFY | 已存在 (PASS) |\n\n## 自验证评分表\n\n| # | 评分项 | Q1.1 | Q1.2 | Q1.3 | Q1.4 | Q1.5 |\n|---|--------|------|------|------|------|------|\n| 1 | 目标具体到文件和代码动作 | Y | Y | Y | Y | Y |\n| 2 | 变更清单列出每个文件操作 | Y | Y | Y | Y | Y |\n| 3 | CREATE 文件包含完整代码 | Y | Y | Y | Y | Y |\n| 4 | MODIFY 文件指定插入锚点 | N/A | N/A | N/A | N/A | Y |\n| 5 | 所有 import 给出完整路径 | Y | Y | Y | Y | Y |\n| 6 | 方法体是完整实现 | Y | Y | Y | Y | Y |\n| 7 | 验收标准可粘贴执行 | Y | Y | Y | Y | Y |\n| 8 | 依赖关系与拓扑排序一致 | Y | Y | Y | Y | Y |\n| 9 | 边界限制列出具体禁止项 | Y | Y | Y | Y | Y |\n| 10 | 合约明确产出/消费关系 | Y | Y | Y | Y | Y |\n| 11 | 预判坑点基于代码分析 | Y | Y | Y | Y | Y |\n| 12 | 反模式警告列出具体禁止 | N/A | N/A | N/A | Y | Y |\n| 13 | CREATE 的 import 风格一致 | Y | Y | Y | Y | Y |\n| 14 | MODIFY 锚点唯一 | N/A | N/A | N/A | N/A | Y |\n| 15 | 回滚方案具体到命令 | Y | Y | Y | Y | Y |\n\n所有 Quest 评分均 >= 13/15 (>= 10 通过)。\n\n**注意**: Quest 1.1 中 `IntentType.REFECTOR` 属性名是一个已知拼写差异（属性名 REFECTOR vs 值 'refactor'）。在 JavaScript 中这不会导致运行时错误，因为外部使用字符串值 `'refactor'`，内部使用 `IntentType.REFECTOR` 属性名。如果你希望属性名也叫 `REFACTOR`，在 Quest 1.1 的 `IntentType` 枚举和 `detectIntentType` 函数中统一修改即可。\n\n## 风险汇总\n\n| 级别 | Quest | 建议 |\n|------|-------|------|\n| LOW | Q1.1, Q1.2, Q1.3 | 可安全并行执行 |\n| MEDIUM | Q1.4, Q1.5 | 必须等 Q1.1-1.3 完成 |\n\n**建议执行顺序**：Q1.1 + Q1.2 + Q1.3（并行） -> Q1.4 -> Q1.5\n\n---\n\n## 模式卡数据\n\n<!--PATTERN_CARDS_START-->\n```json\n{\n  \"cards\": {\n    \"src/index.js\": {\n      \"package\": \"auto-cli (ES Modules)\",\n      \"import_style\": \"chalk -> ./installer -> ./prompts -> ./utils -> ./logger -> ./config -> ./mcp-installer -> ./skills/*\",\n      \"export_pattern\": \"export async function runXxx(options)\",\n      \"class_annotations\": \"N/A (function exports)\",\n      \"method_pattern\": \"export async function runXxx(options) { showBanner(); ... }\",\n      \"return_pattern\": \"void (CLI output via console.log + chalk)\",\n      \"key_imports\": [\"chalk\", \"./installer.js\", \"./prompts.js\", \"./utils.js\", \"./logger.js\", \"./config.js\", \"./skills/skill-catalog.js\", \"./skills/skill-installer.js\"],\n      \"cli_integration\": \"functions imported by bin/cli.js for Commander.js registration\"\n    },\n    \"src/utils.js\": {\n      \"package\": \"auto-cli (ES Modules)\",\n      \"import_style\": \"os -> path -> fs-extra -> child_process -> url\",\n      \"export_pattern\": \"export function xxx() / export const COMPONENTS = {}\",\n      \"key_imports\": [\"os\", \"path\", \"fs-extra\", \"child_process\", \"url\"],\n      \"constants\": \"COMPONENTS{agents,rules,commands,skills,hooks,mcpConfigs,knowledge}\"\n    },\n    \"src/config.js\": {\n      \"package\": \"auto-cli (ES Modules)\",\n      \"import_style\": \"url -> path\",\n      \"export_pattern\": \"export const XXX = value / export default {}\",\n      \"key_constants\": [\"PROJECT_ROOT\", \"DOCS_URL\", \"LOG_LEVEL\", \"DEFAULT_MAX_RETRIES\", \"DEFAULT_TIMEOUT\", \"CLAUDE_DIR\"]\n    },\n    \"src/logger.js\": {\n      \"package\": \"auto-cli (ES Modules)\",\n      \"import_style\": \"chalk\",\n      \"class_pattern\": \"class Logger { constructor(options) } with debug/info/warn/error/success/log/infoRaw methods\",\n      \"export_pattern\": \"export { Logger, logger, LOG_LEVELS } / export default logger\",\n      \"singleton\": \"const logger = new Logger({ level: INFO, prefix: 'auto-cli' })\"\n    },\n    \"src/skills/skill-types.js\": {\n      \"package\": \"auto-cli (ES Modules)\",\n      \"import_style\": \"(无外部依赖，纯定义文件)\",\n      \"exports\": [\"SkillManifest typedef\", \"SKILL_DOMAINS(10个领域)\", \"SkillSource{BUILTIN,COMMUNITY,CUSTOM}\", \"inferDomain(keywords)\", \"parseSkillManifest(content,path,source)\"],\n      \"domain_ids\": [\"requirements\", \"engineering\", \"debugging\", \"testing\", \"data\", \"ml\", \"science\", \"math\", \"multimedia\", \"integration\"]\n    },\n    \"src/skills/skill-catalog.js\": {\n      \"package\": \"auto-cli (ES Modules)\",\n      \"import_style\": \"node:path -> node:fs/promises -> fs-extra -> ../logger -> ./skill-types\",\n      \"class_pattern\": \"class SkillCatalog { constructor(options); scan(); list(); search(); filterByTags(); get(); has(); getStats(); }\",\n      \"index_pattern\": \"Map<name,manifest> + Map<tag,Set<name>> + Map<domain,Set<name>>\"\n    },\n    \"src/skills/skill-installer.js\": {\n      \"package\": \"auto-cli (ES Modules)\",\n      \"import_style\": \"node:path -> node:fs/promises -> fs-extra -> ora -> chalk -> ../logger -> ../utils\",\n      \"class_pattern\": \"class SkillInstaller { constructor(options); install(); uninstall(); update(); installBatch(); listInstalled(); isInstalled(); }\"\n    },\n    \"src/context/context-injector.js\": {\n      \"package\": \"auto-cli (ES Modules)\",\n      \"import_style\": \"node:path -> fs-extra -> ../logger\",\n      \"class_pattern\": \"class ContextInjector { constructor(projectDir); collect(task, options); listPresets(); clearCache(); }\",\n      \"preset_function\": \"export function recommendPreset(taskDescription) -> CONTEXT_PRESETS[key]\"\n    },\n    \"src/loop-state-machine.js\": {\n      \"package\": \"auto-cli (ES Modules)\",\n      \"import_style\": \"path -> fs-extra\",\n      \"state_pattern\": \"LOOP_STATES{INTAKE,CONTEXT,DECOMPOSE,EXECUTE,VERIFY,RECOVER,SUMMARIZE,PERSIST}\",\n      \"exports\": [\"LOOP_STATES\", \"createLoopState\", \"canTransition\", \"transitionLoopState\", \"advanceLoopState\", \"formatLoopState\", \"saveLoopState\", \"loadLoopState\"]\n    },\n    \"bin/cli.js\": {\n      \"package\": \"auto-cli (ES Modules)\",\n      \"import_style\": \"commander -> chalk -> path -> ../src/index -> ../src/utils -> ../src/config -> ../src/knowledge/knowledge-steward -> ../src/loop-state-machine\",\n      \"cli_pattern\": \"program.command('xxx').description().option().action(async (options) => {})\",\n      \"command_groups\": [\"install\", \"update\", \"uninstall\", \"list\", \"docs\", \"skills{list,search,install}\", \"loop{init,status,next,resume}\", \"save{insight,list,search}\"]\n    }\n  }\n}\n```\n<!--PATTERN_CARDS_END-->\n\n---\n\n以上是完整的 Quest Map。确认后 PHASE 3 只需要按 Quest 顺序复制代码到指定路径即可。\n\n**补充说明**：有一个拼写问题需要你决定 -- `IntentType.REFECTOR`（属性名）vs 值 `'refactor'`。在 `detectIntentType` 函数中 `return IntentType.REFECTOR;` 与枚举定义 `REFECTOR: 'refactor'` 是一致的，但属性名不太自然。如果你希望改为 `REFACTOR: 'refactor'`，只需修改两处：(1) 枚举定义中 `REFECTOR` -> `REFACTOR` (2) detectIntentType 中 `IntentType.REFECTOR` -> `IntentType.REFACTOR`。"
  },
  {
    "type": "text",
    "text": "agentId: a34e755588109dd5a (for resuming to continue this agent's work if needed)\n<usage>total_tokens: 84513\ntool_uses: 25\nduration_ms: 521599</usage>"
  }
]
